# Glipra — Build Progress
# Full history of what has been built. Not needed by Claude during coding.
# Update this file at the end of each session.
# Last updated: 2026-06-11 (session 56 — Dark-mode polish: check-in intensity scales + filling-glass water, merged Dose medication card, tighter search row; session 55 — Today Fuel hero card: merged Readiness + Protein + fiber + micros; session 54 — Daily check-in reminder notification; session 53 — Cascade D: seeded foods table + Search database + AI wrong-food fix + code-review hardening)

---

## Current Backlog (as of session 42 — `origin/master` @ `de9803c`)

### Tier 2 — ready to ship next (one session each)
| Priority | Item | Notes |
|---|---|---|
| **1** | **Rescan button** | Re-run recognize-food on same photo without re-snapping. Cache base64 in state; "Try again" in error path is half-done. ~1 hr. |
| ~~2~~ | ~~Wrong food? Search database~~ ✅ SHIPPED session 53 (entry 100) | Built local-first against the seeded `foods` table (Cascade D) instead of OFF name search: "Wrong food?" link in the AI review sheet patches the form from a pharmacist-curated food, zero AI cost. |

### Oral GLP-1 — Phases 1, 2 & 3a SHIPPED
- **Phase 1** (entry 77, `3fb9ba8`): route foundation — schema, oral phase calculator, readiness reskin, onboarding fork, Today wiring.
- **Phase 2** (entry 78, `e5fd8a9`): Dose Window 30-min absorption timer + daily adherence streak + route-aware oral notifications. The differentiator no competitor has.
- **Phase 3a** (entry 79, `6420b9a`): Food-logger coupling — absorption window note on the Nutrition Log screen.

**Phase 3 remaining (one session each):**
| Item | Notes |
|---|---|
| ~~Oral technique education card~~ ✅ SHIPPED session 42 (entry 81) | Empty-stomach rule card (`oral-empty-stomach`, tier 1, route `oral`). Content cards are now route-aware (`getActiveCardsForRoute`) so oral-only cards never leak to injection users; tier-1 cards get a dual top+bottom disclaimer in the sheet. 11 Vitest. |
| ~~visit-prep oral variant~~ ✅ SHIPPED session 42 (entry 82) | Route-aware DOSE ADHERENCE summary (screen + PDF + AI questions) for oral users vs INJECTION CYCLE for injection. Both edge functions branched (backward-compatible) + redeployed. New `visit-prep/summary.ts` pure helper, 11 Vitest. |
| ~~Oral PK curve~~ ✅ SHIPPED session 42 (entry 84) | `useMedicationLevelCurve` is route-aware: oral reads `oral_dose_logs`, daily interval, normalized unit dose → a **relative** concentration curve (y-axis "Relative level", not mg). MedLevelBanner + `/medication-level` un-gated and route-aware. Calculator reused unchanged. **Oral Phase 3 COMPLETE.** |
| ~~`window_respected` capture~~ ✅ SHIPPED session 42 (entry 83) | Clear-state confirm prompt ("any food/coffee/drinks in those 30 min?") writes true/false to `oral_dose_logs.window_respected`; the adherence streak is now technique-aware (an explicit `false` day breaks the run, `null`/`true` count). 24 Vitest. **Only the oral PK curve remains in Phase 3.** |

**On-device test still pending:** onboard an oral account → Today shows the Dose Window card → "Took it" → countdown → clear state → adherence pill increments next day. Needs a dev build session with an oral test account.

### Dose Tab + Today De-Dup — Direction B redesign, Phases 1-4 COMPLETE (sessions 43-46, entries 87-90)
Consolidated all dose/adherence into a route-aware **Dose** tab in bottom-nav slot 2 (replaces the
injection-only Sites tab; oral users gain it). Today now shows the dose **once**. Full detail in
entry 87 + the ARCHITECTURE decisions row (2026-06-07).
- **Nav:** slot 2 = Dose for both routes (Pill icon oral / Syringe injection); `injection-sites` kept
  hidden (`href:null`) for old deep links; `TAB_ROOTS` → `/dose`.
- **Hub** (`src/features/dose/dose-screen.tsx`, route `(app)/dose.tsx`): route-aware, composes existing
  components + tier-1/2 disclaimers; detail screens stay pushed routes reached from the hub.
- **Today de-duped:** removed phase-badge metric / cycle strip / shot-day-prep / med-level banner;
  oral keeps DoseWindowCard (gated off when discontinued), injection gets one smart row
  (`selectInjectionDoseRow`); protein ring full-width.
- **Protein ring:** "Set your target" empty state (was "of 0g") + spacing fix.
- **Pure helpers + Vitest:** `recent-doses.ts` (8), `smart-dose-row.ts` (5). No migration (all columns existed).
- **Adversarial review:** 6 confirmed findings, all fixed (discontinued-oral gate, rotation ES
  localization, duplicate "YOUR CYCLE" label, orphaned key, legacy-screen ES, "Next dose in 0d" boundary).

**Phase 2 SHIPPED (session 44, entry 88):** route-aware `RemindersPanel` (`src/features/dose/reminders-panel.tsx`)
replaces both reminders stubs in the hub. Oral → oral-dose toggle + editable time picker + absorption
info + protein toggle; injection → injection toggle + protein toggle. Reuses `useNotificationSettings()`;
new `src/features/dose/api.ts` (`useUpdateDoseTime`) writes `dose_time_local` + reschedules the daily
oral reminder. 11 jest-expo tests. No migration. Settings toggles unchanged.

**Phase 3 SHIPPED (session 45, entry 89):** route-aware `AdherenceCalendar` (`src/features/dose/adherence-calendar.tsx`)
replaces the oral 7-day strip and is added to the injection face. Oral = 4-week daily-dot grid +
technique-aware day streak (reuses `computeDoseAdherenceStreak`); injection = 8-week weekly-marker grid
+ on-time week streak + on-time % (new `injection-adherence.ts` calculator), "not enough data yet"
below 2 logged shots. Lightweight inline day-detail on tap. Two pure helpers (`injection-adherence.ts`,
`adherence-calendar-data.ts`) + 30 Vitest + 6 RTL. No migration.

**Phase 4 SHIPPED (session 46, entry 90):** Settings protein-target editor
(`src/features/protein-target/protein-target-editor.tsx` + route `(app)/protein-target.tsx`) closes the
empty-state loop. Re-collects body inputs, recomputes the floor live via pure `previewProteinFloor`
(reuses the Rule-4 `calculateProteinFloor`), persists it; Tier-1 disclaimer + acknowledgment (Rule 5/8).
Reachable from Settings → Body Metrics and by tapping the Today protein ring. Adversarial review found +
fixed 3 issues (unit-race data corruption x2, stale-phase recompute). 10 Vitest + 7 RTL. No migration.

**Deferred Phase-4 polish (not built, optional):**
| Item | Notes |
|---|---|
| Richer in-hub PK / titration viz | Inline sparkline + days-to-steady-state in the Dose hub (enhances the working MedLevelBanner). |
| Visit-prep hub link | A "Prep for your visit" ActionRow on the Dose hub → `/visit-prep` (today Settings-only). |
| ~~Stale `phase` column fix~~ ✅ FIXED (session 47, `491ea39`) | Shipped as part of the Metro bundler crash fix. `update-status-screen.tsx` now co-writes the derived `phase` alongside `medication_status` (same derivation as onboarding) + invalidates `['today-profile']`. +4 jest-expo tests. Stops `profile.phase` drifting app-wide. |

**On-device test pending (Phase 1):** oral acct → bar Today/Dose/Nutrition/Progress/Coach, Today shows
the dose once, Dose tab renders the oral hub; injection acct → Dose tab in slot 2 (no Sites tab),
add/edit shot still work; protein ring shows "Set your target" when no floor.

**On-device test pending (Phase 2):** oral acct → Dose tab reminders panel: toggle oral reminder on,
tap time row, change time → persists + reschedules; injection acct → injection + protein toggles work.

**On-device test pending (Phase 3):** oral acct → Dose tab shows the 4-week calendar + day-streak
header, dots match logged days, tap a day → inline detail; injection acct → 8-week calendar + on-time %
+ week-streak, <2 shots shows "not enough data yet", a missed week shows a red marker.

**On-device test pending (Phase 4):** Settings → Body Metrics → Protein target opens pre-filled; edit a
field → floor updates live; acknowledge + Save → Today ring shows the new floor; tap the ring → editor
opens; a null-floor account shows "Set your target" and the tap routes correctly. Verify on a kg/metric
account (the unit-race fix): open from Today, Save WITHOUT editing → stored weight/floor stay correct.

### Scan Accuracy & Cost Cascade — committed roadmap (assessed session 40)
Target the accuracy cascade: cheapest + most accurate inputs first, fewest expensive AI calls.
**9 of 13 strategy points already shipped** (barcode-first, model routing 4o/mini, smarter prompts,
userComment, portion multiplier, user_food_defaults, rate limit, free-tier gating, mock AI).
Only the items below remain. Build order = leverage-first, one feature per session.

| Order | Item | Cost/effort | Notes |
|---|---|---|---|
| ~~**A**~~ | ~~**"Log again" from history**~~ ✅ SHIPPED session 40 (c3bba40) | FREE | Recent Foods horizontal quick-add bar above Today's Log. `deriveRecentFoods` (freq-desc + recency tiebreak, cap 8), `relogFoodEntry` preserves source, one-tap + haptic + check flash. 10 Vitest. **Next: B.** |
| ~~**B**~~ | ~~**Low-confidence confirmation nudge**~~ ✅ SHIPPED session 42 (entry 80) | FREE, no new AI | When `confidencePercent` < 55, AIReviewSheet shows an amber "Double-check this one" banner + amber ring on protein + serving. Pure helper `shouldShowLowConfidenceNudge` + 7 Vitest. Microcopy only, not clinical. **Next: C.** |
| ~~**C**~~ | ~~**Prompt enhancement: dietary pattern + allergens**~~ ✅ SHIPPED session 42 (entry 86) | FREE prompt change | `dietary_pattern` now persisted (was collected + dropped) + passed to recognize-food as anonymized prompt context (constraining diets only). `allergens` column added ahead of its collection UI; voice path deferred. 6 Vitest. **Next: D.** |
| **D** | **Seeded ~200 GLP-1 foods table + pre-AI lookup** | New migration + RLS (Rule 7) | Local verified-food lookup before any AI call. Pharmacist-curated data. Merges with backlog item "Wrong food? Search database." |
| **E** | **Global anonymized AI recognition cache** | Biggest effort; design pass first | New table keyed by normalized food description, RLS-careful (no cross-user PII leak), lookup-before-OpenAI + write-through. Compounding moat: cheaper + more accurate as user base grows. |

Note: backlogged **Rescan** (Tier 2 #1) is still useful for the "AI got it wrong, try once more" case,
but it spends another AI call — schedule it AFTER A/B since the cascade reduces how often Rescan is needed.

### Attorney-review queue — BLOCKS preview/production
| Item | Status |
|---|---|
| `ai-coach` prompts + keyword blocklist | Pending attorney |
| `EscalationCard` copy | Pending attorney |
| `daily-guidance` edge function prompts | Pending attorney |
| `pro_insight.subline_*` copy | Pending attorney |
| Delete-account modal copy | Pending attorney |
| `ai_privacy.*` copy (entry 73) | Pending attorney |
| **Oral dosing copy** — `oral_phase.*`, `readiness.headlines.oral_*`, `readiness.tips.oral_*`, oral injection-day onboarding screen copy | **Pending attorney — session 41 (Phase 1)** |
| **Oral Dose Window copy** — `oral_dose.*` (absorption-window timing, empty-stomach guidance) + the two oral notification bodies (`oral-dose-reminder`, `oral-absorption-clear` in `notifications.ts`) | **Pending attorney — session 41 (Phase 2)** |
| **Absorption note copy** — `absorption_note.*` (food-logger note shown during 30-min window) | **Pending attorney — NEW (session 42, Phase 3a)** |
| **Oral technique card copy** — the `oral-empty-stomach` content card (title, keyTakeaway, body) in `src/features/content-cards/data.ts` (clinical medication-administration guidance) | **Pending attorney — NEW (session 42, Phase 3 technique card)** |
| **Oral visit-prep questions** — `STATIC_QUESTIONS_ORAL` (`visit-prep.tsx`), `PRESCRIBER_QUESTIONS_ORAL` (`generate-visit-pdf`), `MOCK_VISIT_PREP_QUESTIONS_ORAL` (`mockAI.ts`) — pharmacist-authored adherence-framed prescriber questions | **Pending attorney — NEW (session 42, Phase 3 visit-prep oral)** |
| **Oral window confirm copy** — `oral_dose.confirm_*` (`en.json`/`es.json`) — the post-absorption empty-stomach technique check | **Pending attorney — NEW (session 42, Phase 3 window_respected)** |
| **Oral med-level banner copy** — `med_banner_oral.*` (`en.json`/`es.json`) — oral PK phase headlines/pills on the Today banner | **Pending attorney — NEW (session 42, Phase 3 PK curve)** |
| **Dose tab copy** — the `dose.*` namespace (hub disclaimers, building/empty/discontinued/overdue/reminders, rotation labels + tips, screen title/subtitle/disclaimer) + `today.dose_row_*` (`en.json`/`es.json`) — educational dose-adherence + injection-technique copy that defers to prescriber | **Pending attorney — NEW (session 43, Dose tab Phase 1)** |
| **Dose reminders copy** — `dose.reminders_intro`, `dose.reminders_time_label`, `dose.reminders_time_tap`, `dose.reminders_absorption_info` (`en.json`/`es.json`) — educational reminder-panel copy that defers to prescriber (no dosing advice) | **Pending attorney — NEW (session 44, Dose tab Phase 2)** |
| **Dose calendar copy** — the `dose.calendar_*` namespace (18 keys: label, streak/on-time stat labels, day/week units, legend, empty + not-enough states, day-detail status strings) (`en.json`/`es.json`) — educational adherence-calendar copy, no dosing advice | **Pending attorney — NEW (session 45, Dose tab Phase 3)** |
| **Protein-target editor copy** — the `protein_target.*` namespace (`en.json`/`es.json`), especially `protein_target.disclaimer` (the Tier-1 inaccurate-inputs warning — EN reuses the onboarding wording verbatim; ES is a new translation) + `acknowledge`. Liability-critical (Rule 5). | **Pending attorney — NEW (session 46, Phase 4 protein editor)** |
| **Resistance log copy** — the `resistance.*` namespace (`en.json`/`es.json`), especially `resistance.disclaimer` (Tier-2 educational: "general wellness information, not a personalized exercise prescription... talk to your prescriber or a physician...") + `resistance.aim`/`resistance.met` (the 2-to-3-sessions-a-week framing) + `today.resistance_*`. Educational exercise guidance, defers to physician; no dosing advice. | **Pending attorney — NEW (session 48, Muscle-First Phase A)** |
| **Muscle Preservation Score copy** — the `muscle_score.*` namespace (`en.json`/`es.json`): headline bands, factor labels, the 5 tips, and especially `muscle_score.disclaimer` (Tier-2: "reflects your protein and resistance-training consistency... not a measurement of your actual muscle mass... talk to your prescriber"). A pharmacist-branded clinical-adjacent score; confirm the disclaimer tier + the "muscle preservation" claim framing are defensible. | **Pending attorney — NEW (session 48, Muscle-First Phase B)** |
| **Muscle-Score trend copy** — `progress.muscle_card.*` (label, empty title/body, axis labels) + `progress.tips.muscle` (the Rx advisory: "your muscle score blends protein consistency with how often you train...") (`en.json`/`es.json`). Same clinical-adjacent "muscle" framing as Phase B; educational, defers to prescriber. | **Pending attorney — NEW (session 48, Muscle-First Phase C)** |
| **Iron tracking** — the iron RDA framing (**18 mg** target chosen for the at-risk demographic; confirm vs 8 mg male/post-menopausal, and whether to personalize by sex), the `recognize-food` prompt addition (now asks the AI to estimate iron), and `log.nutrient_iron` (`en.json`/`es.json`). Clinical micronutrient guidance. | **Pending attorney/pharmacist — NEW (session 49, Muscle-First Phase D iron)** |

### Infra / pre-launch blockers
| Item | Notes |
|---|---|
| Apple Developer (org) enrollment | **D-U-N-S ISSUED** (session 40). Enrolling as **Leonava LLC** (org, not individual) at developer.apple.com/programs/enroll — legal name must match D&B exactly. $99/yr. Then Apple verifies the org (days to ~2 wks; may call the D&B phone). Gates everything iOS. |
| Mercury business banking | In progress — Leonava LLC, industry Software. Routing/account numbers needed for the App Store Connect **Paid Apps Agreement** (banking + tax). |
| iOS launch sequence (after Apple approval) | App Store Connect app record (bundle ID must match `app.config.ts`) → sign **Paid Apps Agreement** → create IAP products (Pro $9.99/mo, $79.99/yr, Founder $149 non-consumable) → generate **App Store Connect API .p8 key** → wire RevenueCat iOS app to the `GLiPra Pro` entitlement → `eas build --platform ios` → TestFlight (sandbox-test IAPs) → submit. **Attorney review is the real pre-submission gate** — Apple scrutinizes health apps + the "licensed pharmacist" credential claim + medical disclaimers + the App Privacy data-collection label + HealthKit usage. |
| Email confirmation | Re-enable in Supabase dashboard before public release |
| `.env.production` | All keys empty; populate before first production build |
| Home-screen app label + brand icon/splash "GLiPra" | Requires next native EAS build (label via `env.ts NAME`; brand icons + purple splash/adaptive bg committed `5a5ec84` but only ship on a native rebuild, not OTA) |

### Tier 3 backlog
| Item | Notes |
|---|---|
| **Biometric sign-in (fingerprint/Face ID) — ON HOLD** | Quick-unlock over the persisted Supabase session (first login still uses password/Apple; biometric gates re-entry). `expo-local-authentication` is NATIVE → needs a new EAS build, not OTA. Recommended model: **app-lock over the AsyncStorage session**, gated in root `_layout.tsx` via the existing AppState listener (vs. the heavier `expo-secure-store` hardware-encrypted-token option — security-model + lock-timing decisions deferred). Settings "Security" toggle mirroring `NotificationRow`; `EXPO_PUBLIC_SKIP_BIOMETRIC_AUTH` dev-bypass mirroring `IS_DEV_FORCE_PRO`. No PII, no attorney gate. Explored session 40, paused before planning. |
| Other estimates (alternatives) | GPT-4o prompt + Zod change + Pro-gated UI. Wait for user data showing Rescan isn't enough. |
| Symptom card v2 — phase-correlation bars | Direction C from the symptom redesign. Needs ≥2 full injection cycles of data. |

### Code health (non-blocking)
- ~~NativeWind `className` remnants~~ ✅ RESOLVED session 40 (`173bec4`, `1b0c5f3`). CI's clean install exposed `className` on RN components that local `tsc` masked (stale Windows node_modules). Deleted dead Obytes demos (feed + routes, `onboarding-screen`, `style-demo`, `checkbox`/Radio/Switch); converted inert `className` → StyleSheet in the live Select chain (`modal`/`list`/`caret-down`) + the 404 screen. **Current test counts: jest 54**, **vitest 444** (was 422; +22 from oral GLP-1 Phase 1 — 13 oral-cycle-calculator + 9 readiness oral-factor + display test updates). Zero `className` remains.
- **220 eslint WARNINGS** remain (down from 226; non-blocking — eslint passes on warnings). Top: `max-lines-per-function` (~76, incl. long edge-function handlers), `react-hooks-extra/no-direct-set-state-in-use-effect` (~54), `style/max-statements-per-line`, `react/no-array-index-key`. Burn down opportunistically; CI does NOT fail on them. Future ratchet: `eslint . --max-warnings=<n>` to drive the count down without blocking.
- ~~GitHub Actions CI workflow~~ ✅ DONE session 40 (`550f7a2`). `.github/workflows/ci.yml` runs `pnpm check-all` + `pnpm test:utils` (vitest) on every PR + push to master (ubuntu, Node 22, pnpm 10, frozen lockfile). Gated on the lint-error budget being 0.
- ~~32 lint ERRORS / `check-all` red~~ ✅ RESOLVED session 40. `pnpm check-all` is now GREEN (exit 0): auto-fixed 23 (`3fc2357`), hand-fixed 9 (`e8cc328` — react-hooks purity/memo/set-state, unused var, type-redeclare alias, i18next `{{n}}`→`{{day}}` 2-char-var fix). type-check 0, jest 63, vitest 422.
- ~~6 perpetually-dirty files~~ ✅ RESOLVED session 40. Was NOT tool churn: `supabase/.temp/*` (9 CLI scratch files) was tracked-but-should-be-ignored (`adbe526` — gitignored + untracked); the other 5 (`app.config.ts` + 4 icon PNGs) were a real never-committed brand-icon refresh (purple `#5b21b6` + white G monogram), now committed (`5a5ec84`). Working tree is clean.

### OTA workflow reminder
```
pnpm ota:dev --message "..."          # standard OTA (use this first)
pnpm ota:dev:clear --message "..."    # OTA with cache wipe
```
Both wrap `EXPO_USE_FAST_RESOLVER`. The `--message` is REQUIRED: the scripts run
`eas update --non-interactive`, which rejects a publish with no message (bare `pnpm ota:dev`
fails with "--branch and --message ... are required"). pnpm forwards the trailing
`--message "..."` to eas. Keep the message descriptive (e.g. the feature/entry shipped) so the
EAS dashboard history stays readable. Hardened in entry 70.

---

## Month 3 — AI, Safety & Monetization (in progress)

| # | Item | Status |
|---|---|---|
| 1 | expo-camera — barcode scanner live | ✅ |
| 2 | `recognize-food` edge function (GPT-4o, Zod-validated, 50/day cap) | ✅ |
| 3 | `ai-coach` edge function (GPT-4o-mini, keyword blocklist, 10/day, attorney review gate) | ✅ |
| 4 | `redFlagDetector.ts` — 44 tests, 97.61% branch coverage | ✅ |
| 5 | `<EscalationCard />` — Rule 9 compliant, locked copy, Tier 1 disclaimer | ✅ |
| 6 | Medication level estimator — PK SVG chart, steady-state model, /medication-level screen | ✅ |
| 7 | Prescriber visit prep + PDF — generate-visit-prep + generate-visit-pdf edge functions | ✅ |
| 8 | RevenueCat configured — Android app, SDK key, Test Store products, entitlement. iOS deferred (needs Apple Developer account). | ✅ |
| 9 | Spanish localization — react-i18next in-place reactivity, en/es files, language picker in Settings, language step in onboarding | ✅ |
| 9b | Tab bar restructure — Sites as 4th tab, Log renamed Nutrition, MedLevelBanner moved to Daily Actions, weight field in check-in | ✅ |
| 9c | Unit toggles + date input UX — lbs/kg/ft-in toggles, imperial split inputs, all weight flows metric internally | ✅ |
| 10 | Journey Cards + Shot Day Prep Checklist — milestones, MilestoneToast, share button, all 4 unlock triggers wired | ✅ |
| 11 | Apple Health integration (react-native-health-link) — health-import screen, 90-day dedup, EWMA, Health Connect (minSdk 26) | ✅ |
| 12 | PostHog analytics + Sentry — packages + wrappers installed, keys still empty in .env | ✅ |
| 13 | glipra.com landing page V2 — rebranded, email capture live, GitHub Pages | ✅ |
| 14 | First EAS Android dev build (build ID e0626e84) | ✅ |
| 15 | Cloud Supabase live — full Auth → Onboarding → Today flow on device | ✅ |
| 16 | EAS dev build with com.glipra.development + glipra:// scheme (build ID 860c9b45) | ✅ |
| 17 | Full device flow confirmed — Sign up → Consent → Onboarding → Today on physical Android | ✅ |
| 18 | Visual polish pass — clean clinical design direction | ✅ |
| 19 | AI Photo Recognition + Macro Tracker — Photo tab, editable review sheet, macro card, migration 012 | ✅ |
| 20 | Injection site tracker redesign — form-based Add Shot screen, SiteCode rotation, migration 013 | ✅ |
| 21 | Push notifications — injection reminder + daily protein nudge, Settings toggles, AsyncStorage persistence | ✅ |
| 22 | react-native-health-link 0.2.0 — Health Import screen, 90-day dedup, EWMA integration | ✅ |
| 23 | Progress screen — 5 metric cards, weight trend EWMA chart, 4 date ranges, pharmacist tip | ✅ |
| 24 | Pharmacist content cards — 25/25 cards complete with keyTakeaway + phase tags | ✅ |
| 25 | Phase-aware spotlight card — PharmacistSpotlightCard replaces carousel, ContentCardSheet bottom sheet | ✅ |
| 26 | EAS minSdkVersion fix — withGradleProperties config plugin + @expo/config-plugins as direct dep | ✅ (code fixed; build quota resets Jun 1) |
| 27 | Readiness score redesign — transparent factor card, two-layer architecture, 3 new inputs, action tip | ✅ |
| 28 | F2 Micronutrient Daily Watch — Pro-gated card, 4 nutrients (Mg/Zn/B12/VitD), gap banner, 19 Vitest tests | ✅ |
| 29 | F3 Prescriber Visit PDF — un-stubbed `useGeneratePdf()`, real 28-day protein average wired, share functional | ✅ |
| 30 | Auth redirect fix — stale Obytes `login.tsx` + `login-form.tsx` deleted, `_layout.tsx` redirects to `/(auth)/sign-in` | ✅ |
| 31 | Analytics event instrumentation — 5 missing PostHog events wired (INJECTION_LOGGED, PAYWALL_VIEWED, PURCHASE_STARTED/COMPLETED, RED_FLAG_DETECTED, ONBOARDING_COMPLETED) | ✅ |
| 32 | Shot Day Prep Checklist — 5-item pharmacist checklist, optimistic mutations, gradient hero + Rx badge + done banner | ✅ |
| 33 | Medication Level Estimator — Rule 4 Vitest tests (26 cases) + LevelChart SVG (gradient fill, today dot, injection markers, dark mode) | ✅ |
| 34 | Shot-prep i18n — full `shot_prep` namespace in en/es (14 keys), checklist items + screen strings + pharmacist badge | ✅ |
| 35 | In-app Privacy Policy + Terms of Service screens — `LegalDocScreen` shared component, 14-section privacy policy, 16-section ToS with arbitration/class-action waiver, Settings rows wired to `/legal/privacy-policy` and `/legal/terms-of-service` | ✅ |
| 36 | glipra.com legal pages full rewrite — `docs/privacy.html` (15 sections, WMHMD Act, CCPA/CPRA, Texas TDPSA, subprocessors, retention schedule) + `docs/terms.html` (AAA arbitration, class action waiver, $50 liability cap, Texas law); both DRAFT-bannered for attorney review | ✅ |
| 37 | `docs/legal/` markdown sources — 5 legal docs created: terms-of-service.md, privacy-policy.md, medical-disclaimer.md, subprocessor-list.md, refund-policy.md (all DRAFT, require attorney review) | ✅ |
| 38 | Em dash audit + Spanish diacritics fix — all em dashes removed from user-facing copy across 22 files (translations, pharmacist content cards, onboarding, log screen, legal screens, components); 9 missing Spanish accent marks fixed in `readiness.*` namespace | ✅ |
| 39 | glipra.com feature section expanded — 8 "Also included" cards added to `#solution`: Manual Macro Entry (free), Barcode Scanner (free), AI Photo Recognition (Pro), Shot Day Prep (free), Medication Level Estimator (free), Progress Dashboard (free), Prescriber Visit PDF (Pro), Micronutrient Watch (Pro) | ✅ |
| 40 | Preview APK build for beta distribution — `eas.json` preview profile changed to `distribution: internal`, Sentry source map upload disabled (`SENTRY_DISABLE_AUTO_UPLOAD=true`), all env vars wired; shareable APK link: `https://expo.dev/accounts/waliabdul/projects/glipra/builds/7c9951d5` | ✅ |
| 41 | Voice logging (Killer Differentiator #4) — fully Pro-gated, no free tier. `transcribe-food` Supabase edge function (Whisper transcription → GPT-4o mini food extraction, Zod-validated, 100/day circuit-breaker, static system prompt to prevent injection, service-role `ai_invocations` logging); `voice-recognition.ts` client wrapper + 4 Vitest tests; `VoiceCaptureButton` (expo-av, tap-to-start/stop, unmount cleanup, CANCELLED paywall guard); `PhotoReviewSheet` renamed `AIReviewSheet` with optional `transcript?` prop rendering quoted block; `log.tsx` two-button AI row (Photo + Voice side-by-side above Manual/Barcode toggle); 8 i18n keys en/es; edge function deployed to Supabase. 414 tests passing (352 Vitest + 62 jest-expo). Requires new EAS dev build for on-device testing (expo-av is native). | ✅ |
| 42 | VoiceCaptureButton design token cleanup — DOCUMENTED BUT NOT COMMITTED to `81b2433`. The semantic tokens (`buttonDark`, `recordingBg`, `recordingWave`) were never added to `tokens.ts`/`colors.ts`; `voice-capture-button.tsx` still uses hardcoded hex (`#0F172A` L214, `#7f1d1d` L226, `#fca5a5` L253). Verified absent in 2026-05-30 audit. | ❌ not in code |
| 44 | Nutrition Log AI section redesigned (Voice hero leads, Photo secondary) — VOICE-HERO REDESIGN NOT COMMITTED to `81b2433`. `log.tsx` still uses the `aiRow` two-button layout (L202); the idle state is the compact icon+label+subtitle button, not a hero card. Only the i18n keys (`voice_hero_title`, `voice_hero_subtitle`, `voice_cta`) were committed and are currently dead code. Verified in 2026-05-30 audit. | ❌ not in code |
| 45 | PhotoCaptureButton premium band + free logging note — NOT COMMITTED as described to `81b2433`. `free_logging_note` key exists in en/es but is never rendered; `photo-capture-button.tsx` uses `#4C1D95` as a full card background with AI POWERED/PRO badges, not a slim header band, and there is no green "always free" note below the toggle. Verified in 2026-05-30 audit. | ❌ not in code |
| 43 | expo-av Expo Go crash fix + OTA — CODE FIX NOT PRESENT in `81b2433`. `voice-capture-button.tsx:7` is still a plain `import { Audio } from 'expo-av'` (no `import type`, no `getAudio()` lazy-require wrapper), so the described `requireNativeModule('ExponentAV')` crash guard is absent. The CLAUDE.md PostHog/Sentry blocker cleanup did happen; the OTA push to `development` is not code-verifiable. Verified in 2026-05-30 audit. | ⚠️ partial |
| 46 | Daily AI Guidance (last remaining Pro feature) — fully shipped. Migration `016_daily_guidance.sql` (UNIQUE on `user_id, date`; `injection_phase` nullable; `reasoning_text` for "Why?" tooltip; `prompt_version`). Edge function `generate-daily-guidance` deployed to Supabase: GPT-4o mini, cache-hit check, Zod InputSchema + OutputSchema, FALLBACK_RESULT on parse failure, `ai_invocations` log, nutrition-only scope, nausea/energy-aware (soft foods when nausea >= 4, no exercise on nausea=5), Spanish support, ATTORNEY REVIEW REQUIRED gate. Client: `src/features/daily-guidance/api.ts` (mock gate, 400ms delay), `src/features/daily-guidance/hooks.ts` (staleTime: Infinity, 1/day). `DailyGuidanceCard`: Pro-gated via `<ProGate>`, gradient header, loading/error/guidance states, "Why this?" toggle expands `reasoning_text`, DisclaimerBanner tier=1 (AsyncStorage-backed first-view ack). Suppressed when `medicationStatus === 'discontinued'`. 7 i18n keys en/es. 2 analytics events (DAILY_GUIDANCE_VIEWED, DAILY_GUIDANCE_WHY_TAPPED). `guidanceContext` wired into `useTodayData()`. 5 Vitest tests. Fixed `mockAI.test.ts` to match new `MOCK_DAILY_GUIDANCE` shape. 357 Vitest + 62 jest-expo pass. | ✅ |

| 47 | GDPR data export + account deletion (Tier 0, always-free) — two edge functions: `export-user-data` (service-role reads all 12 user tables → single JSON bundle, inline return, 1/day rate limit, `ai_invocations` log) and `delete-user-account` (`auth.admin.deleteUser` → FK CASCADE wipes all tables). Client `src/features/account/{api,hooks}.ts` + `delete-account-modal.tsx` (type-to-confirm "DELETE"/"ELIMINAR", built on RN Modal + tokens, NOT the dead NativeWind `ui/modal.tsx`). Settings Account section: Export my data (writes JSON → expo-sharing) + Delete account (destructive → modal → deleteAccount → signOut). Removed 4 dead i18n keys (`voice_hero_*`, `voice_cta`, `free_logging_note`); added `settings.export_data`/`delete_account` + new `account` namespace (en/es parity 313=313). 2 analytics events. 6 Vitest tests (account-api). 363 Vitest + 62 jest-expo pass. Privacy policy already covers "portable copy" + in-app deletion (no copy change needed). Both edge functions DEPLOYED to Supabase 2026-05-30. PENDING: attorney review of delete-modal copy before public release; on-device test with a throwaway account. | ✅ |
| 48 | Tooling repaired + real-bug fixes; `pnpm check-all` now GREEN (was crash-broken). Type-check driven 44 to 0: added missing `@react-navigation/native`+`/bottom-tabs`+`/elements` deps; deleted dead `theme-item.tsx` (Obytes ThemeItem remnant); `expo-file-system/legacy` for SDK-54 (readAsStringAsync/writeAsStringAsync/cacheDirectory moved out of main entry, was a runtime throw in voice/export/PDF); created missing `barcode_corrections` table (migration `017`, db push + type regen, RLS) which fixed a silently-broken feature; RevenueCat v10 typing (getPurchasesModule typed as module namespace but returns `.default`); SDK-54 NotificationBehavior; ai-coach `MOCK_COACH_REPLY`; TodayProfile.createdAt; misc. Arabic fully removed (`ar.json` deleted + unwired from resources.ts/utils.tsx). eslint.config.mjs calibrated (ignore `**/*.md`; filename-case allows kebab+pascal+camel; large/risky rules to warn; inline disable prefixes `@typescript-eslint/`→`ts/`). Result: lint 2931→0 errors (226 non-failing warnings), type-check 0, 363 Vitest + 62 jest-expo. | ✅ |
| 49 | GitHub Pages / glipra.com landing restored. Pages serves `master /docs` (Jekyll); when the dosepath app repo took over GLiPra.git, `/docs` got replaced by the unmodified Obytes Astro template (Jekyll cannot build Astro → deploys failing; CNAME dropped → glipra.com 404; site frozen on last good deploy). Fix (`1a14aa5`): restored the static landing into `docs/` (`index.html`, `privacy.html`, `terms.html`, `legal/`, `CNAME`=glipra.com, `.nojekyll`) from the local root docs-wrapper repo; removed the Astro cruft; moved internal planning docs to `dosepath/internal-docs/` (not published). Verified live: glipra.com loads again. | ✅ |
| 50 | Sign In button invisible fix. The shared `Button` (`src/components/ui/button.tsx`, Obytes stub) had no `backgroundColor` and a default-black label, so the default-variant button (sign-in, sign-up, forgot-password, onboarding, feed) rendered black-on-dark = invisible (looked "not working" but onPress was wired). Re-styled against colors.ts tokens via `useTheme()`: default = `colors.primary` fill + `colors.white` label; all variants styled; disabled/loading dim. Regression test added. 63 jest + 363 Vitest, type-check 0, lint 0. Shipped to the installed dev build via EAS Update (OTA) on `development` channel (update group `61ad727c`), no rebuild needed. Commit `a3a95e9`. | ✅ |
| 51 | Post-sign-in flicker loop + white flash fixed. After the button became tappable, live sign-in caused a continuous white-flicker loop (cold reopen was fine). Root cause: Supabase client had `autoRefreshToken: true` but no AppState management, so on sign-in the refresh timer raced the stale persisted session and reused a rotated refresh token (`Invalid Refresh Token: Already Used`), churning the session signIn<->signOut and ping-ponging the `(auth)<->(app)` router. Fix (`5717bf5`): tie auto-refresh to AppState in root `_layout.tsx` (startAutoRefresh on active, stopAutoRefresh on background — the Supabase RN-documented remedy); the churn is now a single transient settle, not a loop. Also made the root `GestureHandlerRootView` background follow the device color scheme (dark on dark devices) instead of a hardcoded light `#f7f9fc`, killing the white flash. OTA to `development`. | ✅ |
| 52 | Post-sign-in blank/stranded-on-sign-in fixed (the real blocker, found via live Metro logs). Logs showed `status: signIn` but `hasAgreed: false` (consent never recorded on this build) — and `(auth)/_layout` returned `<Redirect href="/(auth)/consent">` WITHOUT rendering `<Stack>`, so the consent screen (which lives in that Stack) could never mount → blank/stuck on sign-in. Fix (`02c0000`): navigate to consent IMPERATIVELY via `router.replace('/(auth)/consent')` in a `[status, hasAgreed]` effect while rendering a plain `<Stack>`, so the consent screen mounts. Flow now: sign in → consent → agree (`setHasAgreed(true)`) → `(app)` → Today. Confirmed working on device. Temp `[GLIPRA]` diagnostic logs removed afterward. 63 jest + 363 Vitest, type-check 0, lint 0. | ✅ |
| 53 | Nutrition Log voice-hero / photo-row redesign — ACTUALLY SHIPPED this time (the session 44/45 redesign that was logged-but-never-committed; the 4 "dead" keys are now live). `voice-capture-button.tsx`: idle state restyled into a full-width navy hero card (`#1E1B4B`, 👑 PRO badge, mic, brand-purple waveform, "Speak your meal" / "Voice AI extracts macros instantly" / "Tap to record →" pill); recording/loading/Pro-gate logic untouched. `photo-capture-button.tsx`: the big violet gradient card became a compact white action row (camera circle + "Photo scan" / "AI estimates from image" + amber AI pill + brand PRO pill + chevron); camera/Pro-gate logic untouched (added `useTranslation`). `log.tsx`: the two stack vertically (voice hero, then photo row) instead of side-by-side; emerald "Barcode and manual entry are always free." caption added under the Manual/Barcode toggle. Re-added the 4 i18n keys (`voice_hero_title`, `voice_hero_subtitle`, `voice_cta`, `free_logging_note`) en/es; `photo_row_title/subtitle` already existed. Presentation only. 63 jest + 363 Vitest, type-check 0 (touched files), lint:translations parity. Commit `bd7eb76`, OTA to `development` (update group `d768cfd3`). | ✅ |
| 54 | Micronutrient Watch relocation + frosted "Unlock with Pro" upsell. (a) Logging-first reorder in `log.tsx`: `DailyMacroCard` + `MicronutrientWatchCard` moved from the top into a results cluster BELOW the logging actions (above "Today's log"), so the empty micronutrient state no longer pushes the CTAs below the fold. (b) `micronutrient-watch-card.tsx` turned the Pro gate into a conversion surface — Pro users see the real card only when micros are logged (else `null`, removing the empty microscope state); free users get a new `MicronutrientUpsell` fallback: sample nutrient tiles dimmed to 45% behind a frosted scrim (`colors.surface` @ 0.82, two-layer so text stays crisp; no `expo-blur` → OTA-shippable), 🔒 + "See what your meals are missing" + B12/D/Mg/Zn subtitle + "Unlock with Pro" pill (opens paywall, entitlement `glipra_pro`), labelled "Sample preview". 4 i18n keys (`micronutrient_upsell_title/subtitle/cta/sample`) en/es. 63 jest + 363 Vitest, type-check 0 (touched), parity ok. Commit `059fe5c`, OTA to `development` (update group `8818b5d0`). NOTE: dev build auto-unlocks Pro, so the free teaser path is not visible on-device without forcing `isPro=false`. Pre-existing entitlement-id inconsistency flagged (`pro-gate.tsx` + `photo-capture-button.tsx` use `'GLiPra Pro'`; canonical is `'glipra_pro'`). | ✅ |
| 55 | Paywall entitlement-id standardized to `'GLiPra Pro'` (`b8612f3`). The `isPro` detection (`use-subscription.ts` / `revenue-cat.ts` `ENTITLEMENT_ID`) and the pro-gate/photo paywall calls all used `'GLiPra Pro'`, but `voice-capture-button.tsx` + the new micronutrient upsell passed `'glipra_pro'` — a mismatch that would present the paywall against a non-existent entitlement. User confirmed `'GLiPra Pro'` is the real dashboard identifier, so the 2 paywall calls were changed to match (now all 6 occurrences agree). Updated the CLAUDE.md note (was `'glipra_pro'`). OTA to `development` (group `b2bdd84f`). 63 jest + 363 Vitest, type-check 0. | ✅ |
| 56 | Dead micronutrient empty-state code removed (`a574121`). After entry 54 turned the Pro-empty path into `null`, the card's `emptyState`/`emptyIcon`/`emptyText` styles + the sole consumer of the `log.no_micro_data` key were orphaned. Removed the 3 styles and dropped `no_micro_data` from en/es (parity preserved). No behavior change. 63 jest + 363 Vitest, type-check 0. | ✅ |
| 57 | VoiceCaptureButton hardcoded hex → semantic tokens (`9600292`) — closes the last open item from the session-42 era (entry 42). Added 8 tokens (`voiceHeroBg`, `voiceHeroBadgeBg`, `voiceHeroBadgeBorder`, `voiceHeroCtaBg`, `voiceHeroWave`, `voiceHeroTextMuted`, `recordingBg`, `recordingWave`) to `GlipraColorTokens` + both light/dark palettes (identical values — the hero is an always-dark surface by design, like the hero gradient). `voice-capture-button.tsx` now references `colors.*` exclusively; zero color literals remain. Values byte-identical to before → no visual change. 63 jest + 363 Vitest, type-check 0. | ✅ |
| 58 | expo-av → expo-audio migration (`9514209`). expo-av is deprecated in SDK 54; the voice button was its only consumer. Swapped to the hook-based API: `useAudioRecorder(RecordingPresets.HIGH_QUALITY)` + `record()`/`stop()`/`.uri`, top-level `requestRecordingPermissionsAsync()` + `setAudioModeAsync({ allowsRecording, playsInSilentMode })`; dropped the `Audio.Recording` state for the recorder hook + an `isRecordingRef` for unmount cleanup. Output stays `audio/m4a` so `voice-recognition.ts` + the `transcribe-food` edge function are untouched. Registered the `expo-audio` config plugin in `app.config.ts` with an iOS `microphonePermission` string (NSMicrophoneUsageDescription; Android RECORD_AUDIO auto-added). Removed `expo-av` from package.json. **NATIVE MODULE SWAP → not OTA-able; requires a new dev build.** Kicked EAS Android dev build `76b10461`. type-check 0, 63 jest + 363 Vitest. PENDING on-device test of the new recorder (record→stop→review sheet, permission-denied alert, backgrounding mid-record). | ✅ |
| 59 | Real AI enabled on the development channel (`c1a4a82`). User wanted AI photo recognition working on-device; the pipeline was complete but mock-gated. Flipped `EXPO_PUBLIC_USE_MOCK_AI=false` for the **development** profile only (eas.json `development.env` + `.env.development`); preview/production untouched. **Decoupled the dev Pro auto-unlock from the AI mock flag** — `use-subscription.ts` now forces Pro on `EXPO_PUBLIC_APP_ENV === 'development'` (`IS_DEV_FORCE_PRO`) instead of `IS_MOCK_DEV`, because the Pro-gated photo/voice buttons would otherwise become unreachable once mock went false (no real purchase → paywall). Preview/production still run a live RevenueCat entitlement check. Deployed all 8 edge functions to Supabase (`recognize-food`, `ai-coach`, `transcribe-food`, `generate-daily-guidance`, `generate-visit-prep` + the 3 non-AI) — `recognize-food` had never been confirmed deployed before, which would have made photo return the safe "Unknown food" fallback. `OPENAI_API_KEY` turned out NOT to be set initially (recognize-food logs showed "OPENAI_API_KEY environment variable is missing or empty" → photo errored silently, "Analyzing…" then nothing); root-caused via the dashboard Logs tab and fixed by setting the secret via the Supabase dashboard (2026-06-02). Shipped via EAS Update OTA to `development` (`--clear-cache`, group `7f14f093`). type-check 0, 63 jest + 363 Vitest. **Deliberate dev-only override of the CLAUDE.md legal gate** (attorney review of ai-coach + EscalationCard copy still required before preview/production/public; real AI now bills the OpenAI account, $20/mo dev cap — revert by setting the dev mock flag back to true). **CONFIRMED working on device 2026-06-02** — photo recognition returns real GPT-4o reads of the actual meal; voice also verified on dev build `76b10461`. (Reminder: the OpenAI key was pasted in plaintext during setup → should be rotated.) | ✅ |
| 61 | MedLevelBanner height fix (`25def75`). The medication-level tile in Daily Actions was taller than every other action tile (Daily Check-in / Track Weight / Your Journey) because it stacked a mini PK-curve sparkline above the standard icon+headline+pill+chevron row. Removed the entire `CurveSparkline` component, its SVG imports (`Circle`/`Line`/`Polyline`/`Svg`), unused constants (`AMBER`, `SPARKLINE_W`, `SPARKLINE_H`), the `sparklineRow` style, and the unused `todayOffset`/`injectionIntervalDays` destructure. The card is now a uniform single-row tile; the full curve still lives on the `/medication-level` detail screen it taps into. type-check 0, lint 0 errors, jest 63. OTA group `50eca2d1`. | ✅ |
| 62 | Injection Cycle metric tile background fix (`1a38235`). The "INJECTION CYCLE" tile in TODAY'S METRICS had `phaseAccentBg` (`backgroundColor: colors.primaryLight` = purple tint) making it a different colour from the plain-white Protein Today tile. Removed `styles.phaseAccentBg` from the active card's style array (one-line change in `today-screen.tsx` L421). The phase is still communicated by the 3px `borderTopColor` accent which changes per phase. Both metric tiles now have the same plain white surface. type-check 0, jest 63. OTA group `f0be4245`. | ✅ |
| 63 | Add Shot — pain level dots replaced with a real drag-to-slide slider (`db58d1f`). The Pain Level row on `/add-shot` was 11 tiny tap-only dots (9–12px circles) and hard to hit. Rebuilt `src/components/injection-sites/pain-level-slider.tsx` on `react-native-gesture-handler` v2 (`Gesture.Pan` + `Gesture.Tap` raced via `Gesture.Race`) plus `react-native-reanimated` v4 shared values: 4px track + animated fill bar + 24px brand thumb, 11 subtle tick hairlines, snaps to integers 0..10 with `haptics.selection()` on every crossing, tap-anywhere-on-track jumps and snaps. Same `{ value, onChange }` props → `add-shot.tsx` untouched. `accessibilityActions: increment/decrement` so TalkBack can still bump ±1. All colors via `useTheme()` tokens (`colors.primary`/`border`/`textPrimary`, `radius.full`, `shadows.sm`) — zero hex literals. Both deps are already native-compiled into dev build `76b10461` so this is OTA-shippable, no rebuild. type-check 0, jest 63. OTA group `2b269420-a7dc-4181-8d67-bc1d519b44aa`. | ✅ |
| 64 | Bottom-tab order swap — Sites moved next to Today (`b4105a0`). User feedback on the new pain slider work meant they were tapping Sites a lot; the Sites tab was the 4th of 5 and inconveniently placed. Swapped Progress and Sites: new order is Today / Sites / Nutrition / Progress / Coach. One-line change to `VISIBLE_TAB_NAMES` in `glipra-tab-bar.tsx` (the ordered tuple that drives the bar) mirrored by the `<Tabs.Screen>` declaration order in `(app)/_layout.tsx`. `TAB_ROOTS` set is order-independent so the hardware-back-exits-app behaviour stayed correct. No icon/route/screen changes. type-check 0, jest 63. OTA group `8fbf9786-3c87-4e17-822f-de3cf782d436`. | ✅ |
| 65 | Pharmacist content cards — premium gradient-hero redesign (`c2d7f3b` + bundler fix `3907172`). User wanted the Today-screen carousel cards to feel more elegant/premium. Picked the "Gradient Hero Band + Quiet Luxury body + Rx monogram" combo after reviewing 5 design directions in a throwaway HTML mockup at `.planning/mockups/content-cards.html` (kept; useful reference). New card anatomy: (a) `LinearGradient` band at top using `gradients.hero` for tier-2 cards (education/tip/milestone) and a new `gradients.warning` for tier-1 (warning); (b) inside the band: cream category pill with tier-coloured text + a read-time microcopy ("X MIN", computed `Math.max(1, round(body.length/1000))` — no schema change) on the left, an Rx monogram (℞) in a cream circle on the right; (c) pure-white body with title-dominant typography (16px/-0.2 letter-spacing) and quiet 13px body text; (d) hairline divider (full-bleed via negative horizontal margin) + text-only "Read the full note →" CTA in the tier colour (no filled button — the gradient does all the visual work alone); (e) tier-1 cards still carry the Rule-8 `DisclaimerBanner` inline. Also: the carousel cards were previously **inert** (no tap handler) — wired a new optional `onPress(card)` prop through `ContentCardView` → `CardsCarousel` → `today-screen.tsx`'s existing `setSheetCard`, so tapping a card now opens the full pharmacist note. `ContentCardSheet` got a matching gradient-hero header so the carousel→sheet transition feels continuous. **Token additions:** `gradients.warning` (amber→deep amber for tier-1) in both light/dark palettes alongside the existing `hero` purple. **i18n:** added `content_card.read_full_note` and `content_card.min` (en/es). **Bundler debugging note:** initial OTA attempts hit a recurring `Error: Mapping is for a position preceding an earlier mapping` during the Android Hermes bytecode step. Resolved by swapping the `<>...</>` Fragment shorthand inside the conditional footer for an explicit `<View>` wrapper (commit `3907172`) — the fragment-in-conditional pattern was tripping Metro's sourcemap merger. Visual output unchanged. type-check 0, jest 63. OTA group `ea1ecee3-1caa-49c5-a6eb-ec3f309aab3a`. | ✅ |
| 77 | Oral GLP-1 Phase 1 foundation (`3fb9ba8`, session 41). Adds oral GLP-1 medications (Rybelsus / oral semaglutide, oral Wegovy, orforglipron) as a first-class administration route. **Schema (migration 018, applied):** `administration_route` (default `'injection'`), `dose_frequency` (finally persisted), `dose_time_local`, `medication_start_date` added to `profiles`; new `oral_dose_logs` table with owner-only RLS. Types regenerated. **Domain types:** `AdministrationRoute`, `OralPhase` (`building / steady_state / dose_due / dose_missed`); `GLP1MedicationId` extended with `semaglutide_rybelsus` + `orforglipron`. **Half-life correction:** `rybelsus: 0.04` (days — clinically wrong, conflated Tmax with elimination) replaced with `semaglutide_rybelsus: 7` (correct — same molecule as injectable) + `orforglipron: 1.1` added. **Oral phase calculator** (`src/features/oral-cycle/calculator.ts`): pure, date-fns, 13 Vitest cases at Rule 4 safety bar. Adherence overlay takes priority over titration position; `dose_due` is the safe default for unknown state. **Readiness reskin — one engine, two route faces:** `injectionPhase` made optional, `doseStatus: OralPhase` added. Oral deltas gentler (±5). Injectable path unchanged. **Onboarding fork:** oral options in the medication picker (route-tagged); injection-day screen branches on route (oral asks dose time + start date, no day-of-week picker); `dose_frequency` finally persisted. **Today wiring:** `fetchTodayProfile` fetches new fields; readiness re-gated on `profile` (not `lastInjectionDate`) fixing the blank-hero bug; `PhaseBadge` reshaped as a discriminated-union component; `GlipraTabBar` hides injection-sites tab for oral users. **i18n:** `oral_phase` namespace + oral headlines/tips in EN + ES. Verified on device: oral medication options visible in onboarding picker. 25 files, 835 insertions. jest 54, vitest 444, type-check 0, lint 0. ATTORNEY REVIEW REQUIRED for oral dosing copy before any non-dev deploy. | ✅ |
| 103 | Dark-mode premium polish: check-in scales + filling-glass water, merged Dose medication card, tighter search row (session 56, client OTA `028082fc`, commit `eccf753`). On-device dark-mode review flagged four unpolished spots; all fixed (client-only, no migration). **(1) Check-in nausea/energy:** rewrote `RatingSlider` from cartoon-emoji squares to an emoji-free **intensity bar-scale** — 5 tappable segments with ascending bar heights, the selected segment highlighted (`primaryLight` + 2px `primary` border) and its bar tinted by a gentle semantic ramp via a new `tone` prop (`'severity'` green->amber->red for nausea, `'positive'` amber->green for energy, using `colors.success/warning/error`); unselected bars stay calm gray. **(2) Check-in water:** replaced the 8-square grid (`💧`/`○` emoji) with a new **`WaterGlass`** vessel that fills with water (reanimated `withTiming` height + a white-opacity surface line) flanked by round **−/+** stepper buttons; `handleDropPress` became `incWater`/`decWater` (clamped 0..8). Added a `colors.water` token to both palettes (`#0ea5e9` light, `#38bdf8` dark). **(3) Dose "YOUR MEDICATION":** the oral path showed "building to steady state" **three times** (green `PhaseBadge` pill + `MedLevelBanner` tile with a hardcoded off-blue `#60a5fa` that did not adapt to dark + an educational card). Merged all three into one **`MedicationCard`** (`src/features/dose/medication-card.tsx`): a themed pulse icon (`colors.primary`, no hardcoded blue), the phase headline (reuses `med_banner_oral.${phase}_headline`), a phase-colored "Day N" chip, a body line (building -> `dose.building_body`, else the `_pill` text), and a "View medication level" CTA -> `/medication-level`. **Oral branch only;** the injection branch has no such redundancy (its `PhaseBadge` lives under INJECTION CYCLE, `MedLevelBanner` is its lone YOUR-MEDICATION tile) so it was left untouched (PhaseBadge/MedLevelBanner/badgeCard/infoCard all still used there -> no removals). **(4) Log:** tightened `FoodSearchRow` vertical padding (16 -> 12) so it reads as a slim entry, not a tall empty box. 1 new i18n key `dose.view_level` EN/ES. RTL tests: `rating-slider.test` (5 segments, selected state, onChange), `water-glass.test` (a11y fill label), `medication-card.test` (testID render + Day chip + tap -> `/medication-level`). tsc 0, lint 0 errors on touched files, **jest 125 (+8), vitest 627**, lint:translations parity. Client OTA `028082fc` (Android + iOS), clean build (no Hermes flake). No new attorney items (bar-scale + water are non-clinical; only the neutral `dose.view_level` copy added). **PENDING on-device (dark + light):** nausea/energy bars (no emoji, selected tinted); the water glass fills + −/+ clamps at 0 and 8; Dose shows ONE medication card (no triple "building", themed icon); the search row is slimmer; check-in still saves nausea/energy/water. | ✅ |
| 102 | Today "Fuel" hero card: merged Readiness + Protein, added Fiber + Micronutrient spots (session 55, client OTA `2ab9fa15`, commit `ca7d1e6`). The Today screen stacked three score-ish cards (Muscle Preservation Score, Readiness, a lone Protein ring under "Today's Metrics") which read as a dump and buried the protein ring. Merged Readiness + Protein into one gradient-hero **`FuelCard`** placed at the **very top of Today, above the Muscle score** (owner choice). **Anatomy:** a `gradients.hero` header (distinct from the flat Muscle card) with the readiness **headline** + a compact **Readiness dial** (SVG ring + score, spring-fill) + a "Pharmacist-designed" trust pill (reuses `today.readiness_trust`); body reuses **`<ProteinRing size={128}>`** as the hero (color bands + spring fill intact; whole row taps to `/protein-target`) with a "{{pct}}% to floor" / "{{grams}} g to go" column; a **"Why?" toggle** (owner picked option C after an A/B/C mockup) that expands the readiness factor rows (dot + label + signed delta) via reanimated `LinearTransition`/`FadeIn`; two **spot tiles** — Fiber (grams + "of 28 g" + slim bar) and Micronutrients (5 status dots B12/VitD/Mg/Zn/Fe + "n of 5 on track" + chevron -> `/log` Micronutrient Watch); the readiness **tip** + a **Tier-2 disclaimer**. **Empty states:** no floor -> ring "Set your target"; no micros logged -> "Log a meal to track". Pure **`fuel-card-data.ts`** (`FIBER_TARGET_G = 28`; `summarizeFiber` green>=80/amber>=50/low<50 with **calm gray (never red)** below target since fiber is not a safety floor; `summarizeMicros` reusing `getNutrientStatus`, on-track = greens) + 13 Vitest cases. Removed the inline Readiness block + the Today's-Metrics protein ring + the readiness count-up animation + orphaned styles/destructures from `today-screen.tsx`. 11 new `today.fuel_*` keys EN/ES (no em dashes). `jest-setup.ts` gained `useAnimatedProps` + `LinearTransition.duration` reanimated mocks (needed to render ProteinRing in tests) — additive, all suites still green. Client-only, no backend/migration, OTA-shippable. tsc 0, lint 0 on touched files, **vitest 627 (+13), jest 117 (+8)**, lint:translations parity. Client OTA `2ab9fa15` (Android + iOS), clean build (no Hermes flake). **ATTORNEY/PHARMACIST QUEUE: the fiber 28 g general-guidance target + the merged-card educational copy (`today.fuel_disclaimer`, "g to go to protect muscle today").** **PENDING on-device:** Fuel card is the first card above Muscle score (light + dark); ring animates + tap -> editor; "Why?" expands/collapses; fiber bar fills toward 28 g; micro dots reflect logged micros + "Log a meal to track" when none; no leftover Readiness / Today's-Metrics duplicates below. | ✅ |
| 101 | Daily check-in reminder notification (session 54, client OTA `9abdafcf`, `34c5517`). A new opt-in local notification `'daily-checkin-reminder'` that fires every day at a fixed **9 AM** ("Daily check-in: How are you feeling today? Take a moment to log your symptoms.") nudging the user toward the Daily Check-in (the nausea/energy/water symptom log). Mirrors the existing `daily-protein-nudge` exactly: `scheduleDailyCheckInReminder()` (a `DAILY` trigger at hour 9) in `src/lib/notifications.ts` + the new id in the `NotificationId` union + the export; `NOTIF_CHECKIN_ENABLED` AsyncStorage key, `checkInEnabled` state + restore-on-mount, and the schedule/cancel toggle branch in `use-notification-settings.ts`; a `NotificationRow` switch added to BOTH Settings -> Notifications AND the Dose-tab Reminders panel (mirroring the protein nudge); `settings.notif_checkin` + `_subtitle` EN/ES. **Off by default** (opt-in; enabling requests notification permission first). The delivered notification body is **English-only** like every other reminder (the native scheduler fires outside React and cannot read i18next; the in-app toggle labels ARE localized). No deep-link routing on tap (no notification routes anywhere in the app today). tsc 0, lint 0 on touched files, jest 109 (+1 check-in toggle test in `reminders-panel.test`), vitest 614, lint:translations parity. Client OTA `9abdafcf` (Android + iOS), clean build (no Hermes flake). | ✅ |
| 100 | Cascade D: seeded GLP-1 foods table + "Search database" + AI wrong-food fix (session 53, migration 022, client OTA `ae905eee`). The foundational data work that unblocks Phase E (what-to-eat protein-gap suggestions). **Migration 022 (owner-pushed):** new public `foods` table with **exactly 200 pharmacist-curated GLP-1-friendly high-protein seeds** (Greek yogurts, cottage cheese, eggs, chicken/turkey, fish/seafood, shakes/powders, tofu/soy, legumes, dairy, cheese, soft nausea-friendly proteins, bars, deli/jerky, lean meats; Spanish `name_es` on ~50 staples; USDA/label-typical values), `protein_density` GENERATED column, trigram indexes (`pg_trgm`), public-read RLS (`SELECT USING (TRUE)`, no write policies — content_cards pattern), and the `food_logs.source` CHECK extended with **`'database'`**. Six recorded spec deviations, headline: micros aligned to the app's tracked set (`vitamin_d_iu` + `zinc_mg` replace the spec's `calcium_mg`, so a seeded food fills exactly what `BarcodeProduct` + the Micronutrient Watch consume); `serving_description NOT NULL`; seed barcodes deliberately NULL (no fabricated EANs). **Client:** pure `food-search.ts` (Zod row schema mirroring `foodLogRowSchema`, `sanitizeFoodQuery` strips the `% _ , ( )` chars that break supabase-js `.or()`/ILIKE, locale display name with an undefined-locale guard, `seededFoodToLogEntry` + `seededFoodToFormPatch` matching `resultToForm`'s exact string formatting); `searchFoods` (`.or(name/name_es ilike)`, protein-dense-first, limit 20, Zod-validated) + `insertDatabaseFoodLog` (source `'database'`); `useSearchFoods` (`staleTime: Infinity` — static table) + `useInsertDatabaseFoodLog` (+ `FOOD_LOGGED_DATABASE` analytics). **CRITICAL widening:** the `foodLogRowSchema` z.enum gained `'database'` (safeParse would otherwise silently DROP logged database rows from Today/Recents). **UI:** dual-mode `FoodSearchSheet` (RN Modal, 300ms debounce, results list with protein highlight, **tap -> preview card with macro + GLP-1-micro grids + "Pharmacist curated" chip -> Log it**); a `FoodSearchRow` entry (photo-row anatomy + new `Search` icon) between Recent Foods and the absorption note on the Log screen; FoodLogRow source badges restructured into a lookup + a neutral `database` badge; **"Wrong food? Search the database"** link in the AI review sheet under the Serving field — select mode patches all 12 form fields via `applyFood` (sets `userEditedRef` so late personal-defaults cannot clobber; rebases the portion multiplier at 1x; `originalAiName` untouched so correction-learning still fires), nested inside the sheet's Modal tree for iOS stacking. Zero AI cost end to end (closes the old "Wrong food? Search database" backlog item AND the pre-AI lookup half of Cascade D). 17 new `log.*` i18n keys EN/ES. Tests: +15 vitest (`food-search`, incl. the undefined-locale regression caught when jest's i18n mock surfaced it) + 5 jest RTL (sheet: results/preview/log-mode insert/select-mode no-insert/min-chars). tsc 0, lint 0 touched, vitest 613, jest 108, parity. Client OTA `ae905eee` on the `ota:dev:clear` retry (Hermes segfault on first attempt). **PHARMACIST REVIEW QUEUE: the 200 seed nutrition values + the "Pharmacist curated" badge wording.** **PENDING on-device:** search row -> "yog" finds yogurts (ES finds "yogur"); Log it -> database badge + counts in ring/macros/watch + appears in Recents; photo scan -> Wrong food? -> fields patch + multiplier rebases; `ai_invocations` unchanged. **Phase E is now unblocked.** **Hardening follow-up (`d3ee075`, OTA `cfa62fa6`):** an xhigh `/code-review` confirmed the feature sound (it verified the portion-multiplier rebase, the `userEditedRef` guard, `originalAiName` correction-learning, the Zod-schema/select match, and all `source`-union consumers as correct) and surfaced 5 low findings; the owner applied 3 — `sanitizeFoodQuery` also strips `"` and `\` (the ILIKE escape char), `useSearchFoods` is gated on `visible` (no stale-result flash on reopen), and select-mode guards a missing `onSelect`. The other 2 are on record: verify a `'database'` insert on the live DB (migration constraint-name assumption), and the localized-name/defaults mismatch (pre-existing). | ✅ |
| 99 | Muscle-First MVP Phase G: Onboarding trim (session 52, client OTA `01966f63`). Pre-launch conversion win — cut friction from the onboarding flow. Client-only, no migration; the protein floor depends on none of the trimmed fields. **Trimmed ~12 screens to a 7-step numbered flow:** `language -> medication(1/7) -> injection-day(2) -> body(3) -> safety(4) -> status(5) -> protein-target(6) -> reveal(7)`. **Dropped 3 steps:** appearance (theme picker — Settings already has a light/dark/system toggle, default `system`, so nothing lost), goals (preserve-muscle/lose-fat — **cosmetic**: never written to the DB, fed no logic, only on the reveal summary), import (MyFitnessPal/Apple Health/Shotsy — all "Coming Soon" alerts, no integrations). **Deferred dietary** (omnivore/vegetarian/... — real: `dietary_pattern` persists + biases AI photo recognition via `buildDietaryContext`/`recognize-food`) to a post-onboarding surface. Deleted `appearance.tsx`/`goals.tsx`/`import.tsx`/`dietary.tsx`; rewired `language->medication`, `safety->status`, `protein-target->reveal`; renumbered the 7 survivors to `/7`. Removed `goal` + `dietaryPattern` from the onboarding store; `reveal.tsx` dropped the goal summary card; `onboarding/api.ts` writes `dietary_pattern: null`; removed 8 orphaned `onboarding.appearance_*` i18n keys. **New dietary surfaces (deferred collection):** a standalone editor `src/features/dietary/dietary-preference-screen.tsx` (+ thin route `(app)/dietary-preference.tsx`, `href:null`) mirroring `update-status-screen` (OptionCards -> `supabase.update({ dietary_pattern })` -> invalidate `['today-profile']` -> back); a new `Utensils` line icon; `TodayProfile.dietaryPattern` (type + SELECT + map); a Today **"Set your eating style"** action-row nudge shown only while `dietaryPattern == null` (vanishes once set); a Settings -> Preferences **"Eating style"** row showing the current value. New `dietary.*` namespace + `today.dietary_nudge_*` + `settings.dietary_pattern` (EN/ES; non-clinical -> no attorney gate). tsc 0, lint 0 on touched files, vitest 598, jest 103 (+4 dietary RTL in a new suite), lint:translations parity. Client OTA `01966f63` (Android + iOS) — published on the `ota:dev:clear` retry after the first attempt hit the Windows Hermes segfault (exit 3221225477). **PENDING on-device:** walk the 7-step flow (no appearance/dietary/goals/import); confirm theme still switchable in Settings; the eating-style nudge appears then disappears after the editor saves; the Settings "Eating style" row reflects the value; AI photo recognition picks up the dietary bias once set. | ✅ |
| 98 | Muscle-First MVP Phase D part 2: free Micronutrient Watch + REMOVE discontinuation & maintenance modes (session 51, client OTA `1bced805`). Two owner-confirmed changes, client-only, no migration, Rule-4 `protein.ts` untouched. **(1) Un-gated the Micronutrient Watch to FREE** (recorded session-49 recommendation: free watch wins reviews, AI extraction stays the Pro upsell). `micronutrient-watch-card.tsx` lost its `<ProGate>` wrapper + the `MicronutrientUpsell` frosted teaser + the PRO badge + orphaned styles; it now renders the real tiles for everyone when micros are logged (via free barcode or Pro AI photo/voice), nothing when not. Removed 4 orphaned `log.micronutrient_upsell_*` i18n keys (EN/ES); moved "micronutrient watch" to the Always-free list in both CLAUDE.md copies; dropped the Pro chip on `docs/index.html` (glipra.com). AI photo/voice capture buttons stay Pro-gated (untouched). **(2) FULLY removed Discontinuation + Maintenance modes** (owner reversed the "discontinuation support" differentiator). Deleted `discontinuation-mode.tsx`, `maintenance-mode.tsx`, `discontinuation-guidance.ts`, `maintenance-guidance.ts`; narrowed `MedicationStatus` from 5 values to **`'starting' | 'active'`** (canonical `today/api.ts` + the inline redeclarations in onboarding `status.tsx` / `update-status-screen.tsx` / `use-onboarding-store.tsx`); stripped every `medicationStatus === 'discontinued'` branch (Today's maintenance + discontinued banners and 3 gating spots; the Dose-hub discontinued card; the Pro-Insight suppression) and the 2 Settings Preferences rows; removed both `Tabs.Screen` registrations; removed 8 orphaned i18n keys (`today.{discontinued_banner,maintenance_banner,injection_discontinued,oral_discontinued}`, `dose.{discontinued_title,discontinued_body}`, `settings.{life_after_glp1,maintenance_mode}`). **Phase model:** every caller of `calculateProteinFloor`/`previewProteinFloor` now passes `phase:'weight_loss'` (the Rule-4 calculator + its `MAINTENANCE_MULTIPLIER` + tests stay intact, just unreachable from the UI); `today/api.ts` **normalizes `medication_status` on read** (anything not `'starting'` -> `'active'`) so legacy DB rows (maintenance/discontinued/tapering) can never violate the narrowed type or reach Settings/the guidance edge fn. `update-status-screen` writes `phase:'weight_loss'` (self-corrects a stale row); its test dropped the maintenance/tapering/discontinued cases and added `starting -> active`. **Adversarial review** (4-dimension workflow, refute-before-count, 24 agents) found 9 confirmed; fixed the real in-scope ones: the normalize-on-read, **pulled the now-false `maintenance-nutrition` content card** (it claimed "GLiPra's maintenance mode adjusts your protein floor by 10%" — a deleted feature; pulled rather than reworded since it is pharmacist copy), and 2 stale "Pro-gated" comments (`log.tsx` + `daily-macro-card.tsx`). Declined: the `protein.test.ts` maintenance cases (kept — `protein.ts` still has the branch; needed for Rule-4 coverage) and `mockAI.ts` "maintenance dose" (valid clinical term, not the app feature, mock-only). **Out of scope -> owner follow-up (task #121):** tighten the migration-010 `medication_status` CHECK constraint + the `generate-daily-guidance` Zod enum (defensive only now that the client normalizes; both need owner-run db push / functions deploy). No migration this session (client-only). tsc 0, lint 0 on touched files, vitest 598, jest 99 (-2 status tests), lint:translations parity. Client OTA `1bced805` (Android + iOS), clean build. **Owner/attorney queue:** discontinuation + maintenance educational content removed; re-add a corrected goal-weight nutrition card if desired (task #120). | ✅ |
| 97 | Onboarding redesign: shared components + neutral-dark palette + AM/PM dose times (session 50, client OTA `de096432`). User reported the onboarding screens looked broken: buttons/cards "invisible once pressed." **Root cause:** every onboarding screen set `<SafeAreaView style={[..., { backgroundColor: gradients.hero[0] }]}>` — the inline purple overrode the screen's real `colors.background`, so the gradient bled past the hero band and painted the WHOLE screen purple; the selected-card state (8% `primaryLight` tint + `primary` purple text) and the purple primary button then rendered purple-on-purple = invisible when pressed. Compounded by a broken **dark palette** (`background #0d0920`, grays remapped to a purple scale, light-purple `textSecondary`) so dark mode was an oversaturated mess app-wide. **Part A — dark palette fix (`tokens.ts` `darkTokens`):** redesigned from all-purple to a Clean-Clinical neutral-dark slate (`background #0f1419`, `surface #1b222e`, `surfaceElevated #252e3d`, neutral gray50->gray900 slate scale, readable `textSecondary #9aa4b2`, `border #2a3344`; `primary` kept lavender `#c4b5fd`, `primaryLight` bumped to 16%; hero gradient now a header-only saturated band `#5b21b6 -> #1d4ed8 -> #0369a1`). Fixes dark mode APP-WIDE (not just onboarding). Updated `tokens.test.ts` to the new intended values. **Part B — shared components (`src/features/onboarding/components/`):** `OnboardingScaffold` (body sits on `colors.background`, killing the bleed; a FIXED `LinearGradient` hero pinned under the status bar via safe-area insets; scroll body; fixed footer on `surface`), `OptionCard` (radio row; selected = 2px brand border + visible `primaryLight` fill + filled radio, **title stays `textPrimary`** so it never goes invisible — the core fix), `ChoiceChip` (solid brand fill + white label when selected), `StepFooter` (primary + optional Back, disabled state). **Part C:** refactored all 12 onboarding screens (language, appearance, medication, injection-day, body, safety, dietary, goals, status, protein-target, import, reveal) onto the shared components, deleting each screen's duplicated inline scaffolding + the `hero[0]` bug. **Presentation only** — every screen's state, persistence (`useOnboardingStore`), navigation, and validation preserved (incl. protein-target's Tier-1 disclaimer + acknowledgment + live floor calc, and reveal's full session-fallback profile save). **Part D:** the oral dose-time grid now renders **12-hour AM/PM** labels via date-fns (`format(new Date(2000,0,1,h,0), 'h:mm a')`); the stored value stays 24h `HH:00`. No copy rewrites beyond the time labels; no new/removed i18n keys (EN/ES parity unchanged) -> no new attorney items. tsc 0, lint 0 on touched files, vitest 598, jest 101, lint:translations parity. Client OTA `de096432` (Android + iOS), clean build (no Hermes flake). **PENDING on-device walkthrough:** all 12 screens in BOTH light + dark (confirm the selected state is visible — the invisible-when-pressed bug — and dose chips show AM/PM); spot-check Today / Dose / Settings in dark to confirm the palette reads app-wide. | ✅ |
| 96 | Muscle-First MVP Phase D (part 1): iron tracking end-to-end (session 49, migration 021, `ab33f34`, client OTA). Adds iron as the 5th GLP-1 micronutrient (iron + hair thinning is a top unmet need for the female-skewing audience). Iron mirrors the existing Mg/Zn/B12/VitD everywhere: migration 021 (`iron_mg` on `food_logs` + `user_food_defaults`, additive nullable); `recognize-food` edge fn (`ironMg` in schema + prompt, redeployed) + both mocks + voice/photo fallbacks; barcode-lookup (OFF `iron_100g` + USDA **id 1089** — corrected the exploration's wrong 1087=calcium); threaded through all types, insert/read paths, recent-foods re-log, food-defaults, `useDailyMacros`, and the AI review sheet (editable Iron field + portion-multiplier scaling). Watch UI gains a 5th iron tile (grid already flex-wraps). Iron RDA = **18 mg** (protective target for the at-risk demographic; pharmacist to confirm vs 8 mg) + gap-banner food tip. EN/ES `nutrient_iron`. **Gating unchanged** (watch still Pro; un-gate is a later session). Manual entry stays macro-only. **Ops:** owner ran `db push` + `functions deploy` (classifier blocks shared-cloud writes for the agent); post-push `gen types` returned a stale schema once (API cache lag), succeeded on retry. tsc 0, jest 101, vitest 598. Client OTA `3c919874` (Android + iOS), clean build. ATTORNEY REVIEW: iron RDA framing + recognize-food prompt change + `log.nutrient_iron`. **Phase D remainder (separate session, task #107): un-gate/surface the micronutrient watch + surface discontinuation mode.** | ✅ |
| 95 | Muscle-First MVP Phase C: Progress reframe + de-dupe (session 48, client OTA via ota:dev:clear, ships B + C). Reframed Progress around the muscle arc and killed the two audit-confirmed redundancies. **New Progress hero = a weekly Muscle-Score TREND:** pure `buildMuscleScoreTrend` (`src/features/muscle-score/trend.ts`) builds one snapshot per Monday-aligned week over the trailing ~10 weeks, recomputing the Phase-B composite "as of" each week (trailing-28d protein + weeks-resolved resistance) via `calculateMuscleScore` + `computeResistanceFrequency`; `useMuscleScoreTrend` composes `useProteinHistoryPerDay(weeks*7+28)` + `useResistanceLogs`; `MuscleScoreTrendCard` draws a compact fixed-0-100 SVG line in the shared `CardShell` + `PharmacistTip`. (Known approximation: historical days use the current protein floor.) **Removed (git rm) the duplicate cards:** `StreakCalendarCard` (dup of Today streak) + `InjectionAdherenceCard` (dup of Dose on-time %). New order: Muscle trend, Weight results, Weight trend, Protein hit-rate, Symptoms. Kept `useInjectionAdherence` (weight chart markers) + `useProteinHistoryPerDay`. **Hid the Today streak empty state** (the original ask): `StreakCard` renders only when currentStreak > 0. Tip registry dropped `streak`/`injection`, added `muscle`; orphaned i18n removed, `progress.muscle_card.*` + `progress.tips.muscle` added (EN/ES). No migration. Tests: 7 Vitest + 3 jest-expo RTL. jest 101, vitest 596, tsc 0, lint 0. ATTORNEY REVIEW REQUIRED for `progress.muscle_card.*` + `progress.tips.muscle`. **Muscle-First arc A->B->C complete.** | ✅ |
| 94 | Muscle-First MVP Phase B: Muscle Preservation Score (session 48, client OTA). The hero metric that makes the app's promise a tracked number: a 0-100 Today card blending PROTEIN consistency + RESISTANCE training, weighted **70/30 (owner decision)**. Pure `calculateMuscleScore` (`src/features/muscle-score/score.ts`) + `buildMuscleScoreCard` (`card.ts`) mirror the Readiness-score two-layer architecture (transparent factor rows + a tip); named score/card (not calculator) to stay out of the vitest Rule-4 coverage gate, fully tested in `src/__tests__/`. **Never penalizes an untracked lever:** protein adherence = hits / days-WITH-DATA over 28 days (counts only with a floor set + >=3 logged days); resistance adherence = the weekly hit-rate from Phase A's `computeResistanceFrequency` (counts only once >=1 week resolved); a missing lever's weight re-normalizes onto the tracked one, so a new protein-logger is not capped at 70 for never training (once resistance resolves a week, it counts -> a never-trainer caps at 70). No data -> invite state, `--` score. Mounted as the HERO above Readiness on Today; self-contained `useMuscleScore()` composes `useProteinHistoryPerDay(28)` + `useResistanceWeekly` (no useTodayData bloat). Rule 8: **Tier-2** disclaimer (derived habits estimate, not a muscle measurement; defers to prescriber). No migration. New `muscle_score.*` i18n (EN/ES). Tests: 17 Vitest (10 score + 7 card) + 3 jest-expo RTL. jest 98, vitest 589, tsc 0, lint 0. ATTORNEY REVIEW REQUIRED for `muscle_score.*`. **Next: Phase C (Progress reframe + de-dupe).** | ✅ |
| 93 | Dev-OTA Supabase creds fix + real-AI owner decision (session 48, `c942ec6`, OTA `4addf304`). A device sign-in showed "Network request failed". Findings: (1) the immediate error is device-side network (the installed build has real creds baked from eas.json; the project is up; an empty-cred bundle would throw "Missing Supabase env vars" instead). (2) **Latent bug:** `ota:dev` runs `eas update` in production mode -> loads `.env.production` (empty Supabase creds); the EAS "development" environment is also empty (so `--environment development` does not help). Every dev OTA this session (incl. Phase A) shipped without creds -> a device pulling one would break auth. **Fix:** `ota:dev`/`ota:dev:clear` now inline `EXPO_PUBLIC_APP_ENV=development` + the dev `EXPO_PUBLIC_SUPABASE_URL` + publishable `ANON_KEY` (already in eas.json, client-safe) via cross-env; re-shipped a healthy update. (3) **Owner decision:** run real AI in dev (`EXPO_PUBLIC_USE_MOCK_AI=false`); owner handles attorney review before any production publish. CLAUDE.md (both copies) Legal-gate + cost notes updated so the gate no longer blocks dev OTAs/builds. `$20/mo` OpenAI cost note kept. | ✅ |
| 92 | Muscle-First MVP re-center + Phase A: resistance-training log (session 48, migration 020, client OTA). **Strategic pivot:** an audit + competitive analysis this session found the app over-built on the commodity (dose/injection tracking, where Shotsy/DoneDose/MeAgain already win) and under-built on its muscle-preservation differentiator; muscle (the headline promise) was never tracked as an outcome. Two redundancies confirmed (protein streak on Today AND Progress; injection on-time % on Dose AND Progress). **User chose** to re-center on muscle + fill gaps + track muscle as an outcome. A phased **Muscle-First MVP roadmap** was written (spine = a Muscle Preservation Score = protein adherence + resistance activity, reusing the Readiness-Score architecture): A resistance log -> B the score -> C Progress reframe + de-dupe -> D expose micronutrients + add iron + surface discontinuation -> E "what to eat" protein-gap suggestions (needs cascade D) -> F hydration + fiber daily -> G trim onboarding. **Phase A shipped:** the resistance signal. New `resistance_logs` table (migration 020, RLS, Rule 7). Pure `computeResistanceFrequency` (`src/features/resistance/frequency.ts`, `RESISTANCE_WEEKLY_TARGET = 2`): **weekly cadence, not a daily streak** — buckets distinct training DAYS into Monday-aligned weeks, scores each resolved past week hit/miss, current week pending (never penalized), distinct-day dedupe; mirrors `computeInjectionAdherence`. New `/resistance` screen (thin-route pattern: screen in `features/`, route re-export, test in `features/` — reinforces the session-47 bundler rule) with a weekly-progress card, one-tap "Log a session" (optional type + duration), recent list with delete, Tier-2 disclaimer (Rule 8). Today gets a Resistance action row ("{n} this week", success-tinted when met) + a new `Dumbbell` icon. New `resistance.*` + `today.resistance_*` i18n (EN/ES). **Tests:** 13 Vitest + 7 jest-expo RTL. jest 95, vitest 572, tsc 0, lint 0 on touched files. ATTORNEY REVIEW REQUIRED for `resistance.*` (educational exercise copy, defers to physician). **No score yet — that is Phase B.** | ✅ |
| 91 | Metro bundler crash fix + `profiles.phase` column drift fix (`491ea39`, session 47, client-only OTA `95c74cee`). **Root cause of crash:** `src/app/(app)/update-status.test.tsx` was inside Expo Router's `require.context` scan over `src/app/`, pulling `@testing-library/react-native` (Node-dependent) into the native bundle and crashing Metro. **Fix:** extracted the full `UpdateStatusScreen` to `src/features/medication-status/update-status-screen.tsx` (named export), made the route a one-line thin re-export, relocated the test to `src/features/medication-status/` (outside `src/app/`, `require.context` never reaches). Metro bundled cleanly (3213 modules, no crash). **Permanent rule added:** never put `.test.*` files under `src/app/`; screen logic + tests live in `src/features/[feature]/`; the route file is only a thin re-export. **Bundled phase-column drift fix:** the relocated screen now co-writes `profiles.phase` (`'maintenance'` for `maintenance`/`tapering`, else `'weight_loss'`) alongside `medication_status` — this was the only post-onboarding writer of `medication_status` and it never updated the derived `phase`, so every downstream reader (readiness, protein guidance, PK calculator) was using a stale column. **Tests:** 4/4 jest-expo RTL assert both columns per save path. No migration, client-only. | ✅ |
| 90 | Settings protein-target editor — closes the protein empty-state loop (`8495a73`, session 46, client-only). Phase 4 of the Dose-tab Direction-B redesign (the one substantive item of the three optional-polish ideas; in-hub PK viz + visit-prep hub link deferred). Phase 1 added a "Set your target" empty state but it was a **dead-end** (`ProteinRing` is presentational, floor only written at onboarding). **New reachable editor** (`src/app/(app)/protein-target.tsx` → `src/features/protein-target/protein-target-editor.tsx`): re-collects weight/height/activity/kidney/pregnancy (unit-aware via `UnitToggle` + `useWeightUnit`/`useHeightUnit`), recomputes the floor LIVE via a new pure `previewProteinFloor` (`preview.ts` — bmi + null-guards + the **unchanged Rule-4 `calculateProteinFloor`**), shows the floor + adjustment badges, and persists `weight_kg/height_cm/bmi/activity_level/has_kidney_disease/is_pregnant/protein_floor_g`. **Does NOT write `phase`** (driven by medication status). **Rule 5 + Rule 8:** Tier-1 disclaimer with the verbatim onboarding inaccurate-inputs warning + an acknowledgment that gates Save. **Two entry points:** a "Protein target" row in Settings → Body Metrics (shows current floor) + the Today protein-ring card is now tappable. `TodayProfile` gains `activityLevel` (type + SELECT + map) for pre-fill. **Adversarial review** (4-dimension workflow, refute-before-counting) found 3 confirmed issues, ALL fixed: **(HIGH x2, same root)** hydration-vs-async-unit-preference race seeded inputs in the default unit before the persisted unit loaded → corrupted saved weight for kg users / blanked height for metric users; fixed by adding a `loaded` flag to `useWeightUnit`/`useHeightUnit` and gating the seed until both load (+1 regression test). **(MEDIUM)** editor recomputed from the drift-prone persisted `profile.phase`; fixed by deriving phase from `profile.medicationStatus` at compute time (matches onboarding). A separate pre-existing bug — `update-status.tsx` never rewrites the `phase` column, so it drifts app-wide — was flagged for its own task. **No migration.** **i18n:** `protein_target.*` namespace + `settings.protein_target` + `today.protein_edit_a11y` (EN/ES parity). **Tests:** `protein-target-preview.test.ts` (10 Vitest), `protein-target-editor.test.tsx` (7 jest-expo RTL). jest 84, vitest 559, type-check 0, lint 0 errors. ATTORNEY REVIEW REQUIRED for `protein_target.*` (EN disclaimer reuses onboarding wording; ES translated). Client OTA to `development`. **🎉 Dose-tab Direction-B Phases 1-4 complete.** | ✅ |
| 89 | Dose tab Phase 3 — adherence calendar + injection on-time streak (`0b71dfe`, session 45, client-only). Adds a route-aware trailing-week adherence calendar to the Dose hub for BOTH faces, plus the first real injection on-time-streak math. **Two pure helpers (pure-helper-first):** `injection-adherence.ts` (`computeInjectionAdherence`) scores each expected dose-day (`firstDose + k*interval`) as on-time (a log within a +/-1 day grace), missed (grace passed, no log), or **pending** (the current open slot, skipped so an in-progress period never breaks the streak or dents the rate); returns `{ currentStreak, longestStreak, onTimeRate, expectedCount, loggedCount }` (streak units = expected doses = weeks for a weekly regimen) + `deriveInjectionIntervalDays` (1/7/14, mirrors the private one in `medication-level/hooks.ts` so the calendar stays independent of the PK hook). `adherence-calendar-data.ts` (`buildAdherenceCalendar`) builds a Monday-aligned trailing-week grid (`weeks*7` cells) with per-cell status: oral ports the proven `recent-doses.ts` rule (taken/broken/missed/none), injection marks logged days `taken` + resolved expected-miss days `missed`. **Component** `adherence-calendar.tsx`: route-aware, self-contained via `useTodayData`; oral = 4 weeks of daily dots + a technique-aware day streak (reuses the existing `computeDoseAdherenceStreak`), injection = 8 weeks of weekly markers + on-time week streak + on-time %, with a "not enough data yet" state below 2 logged days (calculator stays honest, the component gates the display). Lightweight inline day-detail on tap (date + status, dose time / shot site), no modal. Mirrors the protein `StreakCalendarCard` layout + responsive `cellSize`. **Local-calendar-day consistency:** the grid, both builders, and the tap-detail lookups all normalize timestamps via `format(parseISO())` (LOCAL), avoiding the UTC `slice(0,10)` mismatch that would drop a late-night dose on the wrong cell. **Today de-dup continues:** the oral 7-day strip is removed from the hub (the calendar subsumes it); `recent-doses.ts` + its Vitest stay (harmless). **No migration.** Rule 8: calendar is Tier-2 only (hub carries Tier-1). **i18n:** 18 new `dose.calendar_*` keys (EN/ES parity). **Tests:** `dose-injection-adherence.test.ts` (21 Vitest), `dose-adherence-calendar.test.ts` (9 Vitest), `adherence-calendar.test.tsx` (6 jest-expo RTL). jest 77, vitest 549, type-check 0, lint 0 errors. ATTORNEY REVIEW REQUIRED for `dose.calendar_*` (educational, no dosing advice). Client OTA to `development`. **Phase 4 remains:** in-hub PK/titration polish + Settings protein-target editor. | ✅ |
| 88 | Dose tab Phase 2 — reminders panel + oral dose-time editor (`2e8e5ea`, session 44, client-only). Surfaces the already-built notification system into the Dose hub and finally makes the oral reminder time editable. **The gap:** `src/lib/notifications.ts` + `use-notification-settings.ts` already had all four reminders (injection-reminder, oral-dose-reminder, oral-absorption-clear, daily-protein-nudge) but they were buried in Settings, and `profiles.dose_time_local` (the oral reminder time) was set once at onboarding with **no edit UI**; the hub's reminders section was a stub `ActionRow` deep-linking to `/settings`. **New `RemindersPanel`** (`src/features/dose/reminders-panel.tsx`) replaces both stub rows (oral + injection branches) in `dose-screen.tsx`. Route-aware: oral → oral-dose toggle + editable **time picker** (`@react-native-community/datetimepicker` `mode="time"`, already native-compiled since session 23) + absorption-clear info row + protein-nudge toggle; injection → injection toggle + protein-nudge toggle. **Reuse over rebuild:** the panel drives all toggle state through the existing `useNotificationSettings()` and replicates the `NotificationRow` switch pattern from `settings-screen.tsx` as a local sub-component (it was never exported); toggle labels reuse the existing `settings.notif_*` keys (no duplication). **Oral time editor:** new `src/features/dose/api.ts` (`updateDoseTime` + `useUpdateDoseTime` mutation invalidating `['today-profile', userId]`) writes `dose_time_local`; the Postgres TIME string (`HH:mm:ss`) ↔ `Date` round-trip uses date-fns `parse`/`format`/`isValid` (Rule 6, no raw Date math), defaulting to 8:00 AM when null/malformed. On change the panel writes the DB then, if the oral reminder is on, reschedules via `notifications.scheduleOralDoseReminder(time)` in `onSuccess` so the daily trigger updates immediately. **Tests:** `reminders-panel.test.tsx` — 11 jest-expo RTL cases (oral vs injection faces, time-row visibility tied to the oral toggle, absorption-info gating, toggle dispatch); lives next to the component (jest-expo), not `src/__tests__/` (Vitest-only). **No migration** (`dose_time_local` exists since migration 018). **Rule 8:** panel is educational → Tier-2 only; the hub already carries Tier-1 at top. Settings keeps its own toggles unchanged. **i18n:** 4 new `dose.reminders_*` keys (EN/ES parity). 6 files, 71 jest, type-check 0, lint 0 errors. **Attorney gate:** new `dose.reminders_*` copy joins the queue (educational, defers to prescriber; no dosing advice). Client-only OTA to `development`. **Phases 3-4 remain:** adherence calendar + injection adherence streak; PK/titration polish + Settings protein-target editor. | ✅ |
| 87 | Dose tab + Today de-duplication — Direction B redesign Phase 1 (session 43). Consolidated all dose/adherence into a route-aware **Dose** tab in bottom-nav slot 2 (replaces the injection-only Sites tab; oral users gain it; Pill icon vs Syringe picked per `administration_route` in `glipra-tab-bar.tsx`; `injection-sites` kept as a hidden `href:null` route for old deep links; `TAB_ROOTS` updated so hardware-back still exits at `/dose`). **New Dose hub** (`src/features/dose/dose-screen.tsx`, route `(app)/dose.tsx`) composes existing components route-aware: oral → DoseWindowCard + PhaseBadge(oral) + MedLevelBanner(oral) + building card + 7-day recent-dose strip + reminders row; injection → PhaseBadge(injection) + InjectionCycleCard + log-shot CTA + shot-prep (injection day) + `<DoseInjectionRotation>` + MedLevelBanner(injection) + reminders row; discontinued → single paused card → `/discontinuation-mode`. Tier-1 + tier-2 disclaimers (Rule 8). **Site-rotation UI extracted** from `injection-sites.tsx` into shared `src/components/dose/dose-injection-rotation.tsx` (now localized EN/ES); the legacy screen is a thin wrapper, also localized. **Today de-duped:** removed the redundant dose STATUS surfaces (phase-badge metric card, InjectionCycleCard, shot-day-prep card, MedLevelBanner) — protein ring goes full-width; oral keeps DoseWindowCard (the action, now also gated off when discontinued); injection gets ONE smart dose row (pure `selectInjectionDoseRow`) deep-linking to `/add-shot` (injection day / 0-days-left / no cycle) or `/dose` (next dose / overdue). **Protein ring** shows "Set your target" when `protein_floor_g` is null (was "of 0g") + fixed number spacing; the 200 a user saw is the real stored floor at the `ABSOLUTE_CEILING_G` cap, not a placeholder. **Pure helpers + Vitest:** `recent-doses.ts` (8) + `smart-dose-row.ts` (5). **Adversarial review** (5-dimension workflow, 17 agents) found 6 confirmed issues, ALL fixed: discontinued-oral DoseWindowCard gate, rotation-module ES localization, duplicate "YOUR CYCLE" label (new `dose.site_rotation_label`), orphaned `dose.recent_none` removed, legacy injection-sites English localized, "Next dose in 0d" boundary (0-days-left now → log-shot, +1 Vitest). **No migration** (all columns existed). type-check 0, lint 0 errors, jest 57, vitest 519. Client-only OTA. **Attorney gate:** new `dose.*` + `today.dose_row_*` copy joins the queue. **Phases 2-4 remain:** reminders panel + oral dose-time editor; adherence calendar + injection adherence streak; PK/titration polish + Settings protein-target editor. | ✅ |
| 86 | Scan Accuracy & Cost Cascade item C — dietary pattern into the recognize-food prompt (session 42). The cheapest remaining accuracy lever: bias photo food-ID toward how the user eats (a vegetarian's patty-in-bun → plant-based). **The hidden gap:** `dietary_pattern` was collected in onboarding (`dietary.tsx`) but **silently dropped on save** — `saveOnboardingProfile` never wrote it and there was no column (same class as the Phase-1 `injectionFrequency` drop). **Migration 019** adds `dietary_pattern TEXT CHECK(...)` + `allergens TEXT[]` to `profiles` (no new table → no new RLS; profiles already owner-scoped); types regenerated. **onboarding/api.ts** now persists the collected `dietary_pattern` (one line). **Pure helper** `buildDietaryContext` (`src/features/food-log/dietary-context.ts`, 6 Vitest): emits only the **constraining** diets (`vegetarian`/`vegan`/`pescatarian` — omnivore/other/null add no signal + waste tokens) and allergens only when non-empty; returns null otherwise. **Client plumbing:** `getUserDietaryContext(userId)` in `food-log/api.ts` (mirrors `getRecentCorrections`); `usePhotoFoodLog` fetches corrections + dietary in parallel and passes both; `recognize()` gains an optional `dietaryContext` arg spread into the invoke body. `log.tsx` unchanged. **Edge function** `recognize-food`: InputSchema gains optional `dietaryPattern` (enum) + `allergens` (string[]); `buildSystemPrompt` appends a tie-breaker instruction ("when ambiguous, prefer the identification consistent with the diet; do not override clear visual evidence"); backward-compatible, redeployed. **Rule 2:** dietary pattern + allergen flags are categorical preferences, never identifying — no PII to OpenAI; **not in the attorney queue** (food ID, not clinical). **Allergens collection UI + voice (`transcribe-food`) dietary context deferred** (column + edge support already in place → no future migration/redeploy needed). Existing accounts read `dietary_pattern` null (never persisted) → no context until re-onboard; no regression. type-check 0, lint 0 errors (224 warnings), jest 57, vitest 506 (+6). Migration `db push` + edge `functions deploy` to `cuxndkreewlcmijxlgyg` + client OTA to `development`. **Next cascade: D (seeded ~200 GLP-1 foods table + pre-AI lookup).** | ✅ |
| 85 | Adversarial code-review pass over the oral GLP-1 surface + 5 fixes (session 42, client-only). Ran a multi-agent review workflow (12 agents) over the 51 files touched this session: 5 dimension reviewers (route-gating, safety/RLS/liability, correctness, i18n parity, pure-math) each read their file subset in parallel, every finding was adversarially verified (refute-by-default) before counting, then synthesized. **6 raised → 5 confirmed.** Security/liability dimension came back CLEAN (no PII to OpenAI, no condition names leaked, RLS intact, escalation copy untouched). **Fixed all 5:** **(HIGH, promotion blocker) `DoseWindowCard` optimistic state never reset across doses** — the persistently-mounted card kept day-1's `localAnswered`/`localTakenAt` forever, so day-2+ doses silently skipped the technique confirm and showed a false "recorded," leaving `window_respected` null. Fixed with a during-render `prevDoseIdRef` reset (React-recommended adjust-state-on-prop-change; no effect, no lint) + a 3-case RTL regression test (`dose-window-card.test.tsx`) proving the confirm reappears for a new dose id. **(MEDIUM) oral users saw "INJECTION CYCLE" chrome** on the Today metrics card (hardcoded `today.injection_label`/`injection_discontinued`) — added `today.oral_label`/`oral_discontinued`/`no_oral_data` (en/es) and route-selected them. **(LOW) AIReviewSheet** late-arriving personal-defaults could clobber in-progress edits — added an `appliedForResultRef` + `userEditedRef` one-shot guard so defaults prefill a repeat scan but never overwrite after the user types. **(LOW) `generateSteadyStateCurve`** now dedupes `actualInjectionDates` by calendar day internally (defense-in-depth on the Rule-4 file; callers already dedup) + a duplicate-date test. **(LOW) `calculateReadinessScore`** injection `adjustment`/`overdue` could fall through into the oral dose-status branches — split into two independent blocks gated on `injectionPhase === undefined` + a fall-through regression test. type-check 0, lint 0 errors (224 warnings), jest 57 (+3), vitest 500 (+2). No new attorney copy (oral_label is non-clinical chrome). OTA to `development`. | ✅ |
| 84 | Oral GLP-1 Phase 3 — Oral PK curve + route-aware MedLevelBanner (session 42, client-only). **Completes Oral Phase 3.** Brings the medication-level concentration chart, its Today banner, and the `/medication-level` screen to oral users, route-aware. **The curve hook** (`useMedicationLevelCurve`) now branches on `administrationRoute`: oral reads `oral_dose_logs` (90-dose window, daily interval), injection reads `injection_logs` as before. **Normalized/relative curve** (user-confirmed): oral users have no recorded dose anywhere (`oral_dose_logs` has no mg column; `profile.dose_mg` is never set by onboarding — onboarding declares `doseMg` but no screen sets it), so the curve uses a normalized unit dose and the screen shows a **"RELATIVE LEVEL"** percent (of steady-state peak) instead of `~X mg`; the medication badge drops the misleading mg and shows just "daily". The half-lives are correct (Rybelsus 7d, orforglipron 1.1d), so the *shape* — accumulation to steady state + the gentle daily rhythm vs weekly injection peaks — is real; only the scale is normalized. **The Rule-4 safety calculator (`generateSteadyStateCurve`) is reused unchanged** — it already supported daily (`interval=1`) + `actualInjectionDates`; the hook just feeds it oral dose dates. **MedLevelBanner** is a discriminated route/phase prop (`{route:'injection',phase:InjectionPhase}` | `{route:'oral',phase:OralPhase}`) picking `med_banner.*` vs `med_banner_oral.*` + a route-aware empty CTA ("Log your dose to view your curve"). today-screen's `!isOral` gate removed → oral users get the banner (passing `oralCycle.phase`), hidden only when discontinued. **`/medication-level`** route-aware: `PhaseBadge route="oral"` with `oralCycle`, relative summary, oral badge. **i18n** `med_banner_oral` (4 oral phases × headline+pill, en/es parity). **Tests:** +1 calculator assertion (oral daily normalized dose accumulates above a single dose, today positive, all non-negative). No migration, no edge function, no native module. type-check 0, lint 0 errors (224 warnings), jest 54, vitest 498 (+1). ATTORNEY REVIEW REQUIRED for `med_banner_oral.*`. OTA to `development`. **🎉 Oral GLP-1 Phases 1-3 fully complete.** | ✅ |
| 83 | Oral GLP-1 Phase 3 — `window_respected` capture + technique-aware streak (session 42, client-only). Completes the loop Phase 2 deferred: `oral_dose_logs.window_respected` was always logged `null`; now, after the 30-minute absorption window clears, the Dose Window card asks a one-tap confirm ("any food, coffee, or other drinks in those 30 minutes?") and writes true/false. **Capture point = the card's `clear` state gated on `window_respected === null`** (not a transient modal at the absorbing→clear transition — the app is usually closed then, the `oral-absorption-clear` notification covers that): the prompt shows live if the user is watching AND persists for when they return, one code path, no missed captures. Two buttons (**Stayed empty** = true / **Ate or drank early** = false) with optimistic local resolution + a `confirm_done` ack. **Technique-aware streak** (`dose-window.ts`): `computeDoseAdherenceStreak` now takes `{ takenAt, windowRespected }[]`; a day breaks the run **only** on an explicit `false` (acts like a missing day), while `null`/`true` count — so with today's all-null data the streak is byte-identical to before, and it only starts reflecting technique once users answer (never punishes non-answering or honesty). **Data layer:** new `updateOralDoseWindowRespected` (api.ts — the first UPDATE path, user-scoped by id+user_id under the existing RLS owner-update policy) + `useSetDoseWindowRespected` hook (invalidates oral-dose-logs + today-profile, fires new `ORAL_WINDOW_CONFIRMED` analytics). `today/hooks.ts` feeds dose objects to the streak and exposes `oralLastDoseId` + `oralLastDoseWindowRespected` for the card. **i18n:** `oral_dose.confirm_*` (5 keys, en/es parity), educational copy. No migration (column existed since migration 018), no edge function, no native module. type-check 0, lint 0 errors (224 warnings), jest 54, vitest 497 (+4 streak cases: false-breaks-run, mid-history cap, same-day-any-false). ATTORNEY REVIEW REQUIRED for the confirm copy. OTA to `development`. **Oral Phase 3 now complete except the oral PK curve.** | ✅ |
| 82 | Oral GLP-1 Phase 3 — Visit-prep oral variant (session 42, full stack). Makes the Prescriber Visit Prep feature route-aware end to end: oral users get a **DOSE ADHERENCE** summary (medication, treatment status, dosing streak, days since last dose) instead of the injection-framed INJECTION CYCLE (current phase, days since injection) — on the screen, in the AI-generated questions, and on the exported PDF a prescriber reads. Nutrition/weight/symptom spine identical for both routes. **Pure helper** (`src/features/visit-prep/summary.ts`, 11 Vitest): `medicationIdToName` (extracted from hooks + oral med names `semaglutide_rybelsus`/`orforglipron`), `injectionPhaseLabel`, `oralPhaseLabel`, `daysSinceLastDose` (date-fns, Rule 6). **Hooks** (`visit-prep/hooks.ts`): `VisitPrepData` gains `administrationRoute` + oral fields (`oralPhase`, `doseAdherenceStreak`, `daysSinceLastDose`); `useVisitPrepData` reads `oralCycle`/`oralAdherenceStreak`/`oralLastDoseTakenAt` from `useTodayData()` and branches; both edge-function payloads send route-specific fields; mock branch returns `MOCK_VISIT_PREP_QUESTIONS_ORAL` for oral. **Screen** (`visit-prep.tsx`): route-conditional DOSE ADHERENCE vs INJECTION CYCLE card; route-aware `STATIC_QUESTIONS`. **Edge functions** (both redeployed to `cuxndkreewlcmijxlgyg`): `generate-visit-prep` + `generate-visit-pdf` schemas gain `administrationRoute` (defaults to `'injection'` → backward-compatible with existing clients) + optional oral fields; context-building / PDF section / fallback questions branch on route. **System prompt unchanged** (already route-agnostic) — only the anonymized data context differs, so no new attorney-gated system prompt; the oral fallback/mock questions do join the attorney queue. No migration, no RLS, no new native module. type-check 0, lint 0 errors (222 warnings), jest 54, vitest 493 (+11). ATTORNEY REVIEW REQUIRED for the oral prescriber questions before non-dev deploy. Client OTA to `development`; edge functions deployed via `supabase functions deploy`. **Next Phase 3: oral PK curve, `window_respected` capture.** | ✅ |
| 81 | Oral GLP-1 Phase 3 — Technique education card + route-aware content cards (session 42). Adds the highest-value oral pharmacist lesson: the empty-stomach rule (take on an empty stomach with a small sip of water, wait 30 min before food/coffee/other drinks/meds, or absorption drops). **New card** (`oral-empty-stomach` in `src/features/content-cards/data.ts`): `tier: 1`, `cardType: 'warning'`, `route: 'oral'`, inline English (matches the existing 25-card corpus — card bodies are not in the translation files; full content-card ES localization is a separate corpus-wide backlog item). Em-dash-free; pharmacist note defers to the product's printed directions + prescriber/pharmacist. **Route-aware surfacing (the real engineering):** the spotlight selector + carousel both called `getActiveCards()` with no route filter, so a universal oral card would have leaked to injection users. Added an optional `route?: AdministrationRoute` field to `ContentCard` + a pure `getActiveCardsForRoute(route)` helper (cards with no route stay universal → zero regression for injection); `today-screen.tsx` now derives `cardRoute = isOral ? 'oral' : 'injection'` and both the spotlight `useMemo` (new dep) and the carousel read the route-filtered list. Injection users never see `route: 'oral'` cards; oral users see the oral card plus all untagged cards. **Dual disclaimer (liability rule 4):** `content-card-sheet.tsx` now renders a top DisclaimerBanner for `tier === 1` cards (above the title) in addition to the existing bottom one, so clinical cards carry top + bottom; tier-2 keep bottom-only. The carousel tile already rendered the tier-1 inline disclaimer. **Tests:** new `src/__tests__/content-cards.test.ts` (11 Vitest) — route filter (oral card shown to oral, hidden from injection; universal shown to both; sorted), the oral card's tier/route/non-empty-copy/no-em-dash, and corpus invariants (unique ids, unique sortOrder, tier ∈ {1,2}). No migration, no edge function, no AI, no native module. type-check 0, lint 0 errors (222 warnings, +2 non-blocking), jest 54, vitest 482 (+11). ATTORNEY REVIEW REQUIRED for the `oral-empty-stomach` card copy before any non-dev deploy. OTA to `development`. **Next Phase 3: visit-prep oral variant, oral PK curve, `window_respected` capture.** | ✅ |
| 80 | Scan Accuracy & Cost Cascade item B — Low-confidence confirmation nudge (session 42). Turns the AI's self-reported confidence weakness into a trust feature: when `confidencePercent` < 55, the `AIReviewSheet` actively pushes the user to verify the two fields that most affect protein-floor tracking before logging, instead of relying on the passive `~XX%` chip alone. **Pure helper** (`src/features/food-log/low-confidence-nudge.ts`): `LOW_CONFIDENCE_THRESHOLD = 55` + `shouldShowLowConfidenceNudge(percent)` — exclusive threshold (55 does NOT nudge), unknown confidence (null/undefined) returns false so legacy responses never alarm. 7 Vitest (boundary-exhaustive: 54 true, 55 false, 0 true, 100 false, null/undefined false, -1 true, threshold constant pinned). **UI** (`ai-review-sheet.tsx`): reuses the already-computed `confidencePercent` (with `bucketToPercent` fallback); when low, renders an amber `low_confidence` banner below the header subtitle (`accessibilityRole="alert"`, `colors.warningLight` bg + `colors.warning` left rule) and amber-rings the protein `MacroInput` (border-only override, keeps the purple highlight bg/text) + the serving `FieldRow` (amber bottom border) via a new optional `flag?` prop on both sub-components. Design choice locked with user: flag protein + serving only, not all macros (noisy) or a bare banner (un-actionable). **i18n:** new `low_confidence` namespace (2 keys: `banner_title`, `banner_body`), EN + ES parity. **No edge-function / prompt / schema change** — purely a client reaction to a number the AI already returns; no AI cost, no migration, no RLS, no Pro-gate change (review sheet already behind the photo/voice gate). **Microcopy only, not clinical → no attorney gate, no DisclaimerBanner.** type-check 0, lint 0 errors (222 warnings, +2 from the larger AIReviewSheet — non-blocking), jest 54, vitest 471 (+7). **Testing note:** mock confidence is 87% (photo) / 65% (voice) so the nudge won't appear with mock AI on unless `MOCK_MEAL_RECOGNITION.confidence_percent` is temporarily dropped below 55; with real AI it triggers on genuinely ambiguous plates. OTA to `development`. **Next cascade item: C (dietary pattern + allergens prompt enhancement).** | ✅ |
| 79 | Oral GLP-1 Phase 3a — Food-logger coupling (`6420b9a`, session 42). Adds a slim amber informational banner to the Nutrition Log screen that appears whenever an oral GLP-1 user is inside the 30-minute empty-stomach absorption window. Never blocks logging. **AbsorptionWindowNote** (`src/components/log/absorption-window-note.tsx`): self-contained; reads `useTodayProfile` + `useOralDoseLogs` internally (React Query deduplicates); 30s `now` ticker (per-minute resolution is plenty for the log screen — contrast with DoseWindowCard's 1s per-second ticker); session-dismiss via `useState`; returns null for injection users or when window is not absorbing; `accessibilityLiveRegion="polite"` for screen-reader announcement. **Wiring:** mounted as item 3b in `log.tsx` between `RecentFoodsRow` and the manual/barcode mode toggle — squarely in the logging zone so users see it before choosing how to log. **i18n:** new `absorption_note` namespace (3 keys: `title`, `body`, `dismiss_a11y`), EN + ES parity (449=449). All copy educational, no directives ("to get the most out of it, hold off if you can"). No new Vitest needed (all window logic covered by the 20 existing `oral-dose-window.test.ts` cases). type-check 0, jest 54. ATTORNEY REVIEW REQUIRED for `absorption_note.*` copy before preview/prod. OTA to `development`. | ✅ |
| 78 | Oral GLP-1 Phase 2 — Dose Window + adherence (`31e12b8`, session 41, branch `feat/oral-dose-window`). The oral differentiator no competitor has: a 30-minute empty-stomach absorption timer plus a daily-dosing adherence streak. **Pure logic** (`src/features/oral-dose/dose-window.ts`, Rule 4 bar, 20 Vitest): `computeDoseWindow` derives `not_taken / absorbing / clear` from the last dose timestamp + now — stays `absorbing` across midnight so we never imply a second dose, clamps clock skew to a full window, flips to `clear` exactly at the 30-min boundary; `deriveLastDoseDate` (most-recent dose calendar date → feeds `calculateOralPhase`); `computeDoseAdherenceStreak` (consecutive dose days, mirrors the protein streak engine — dedup, future-date filter, live-only-if-today-or-yesterday). **Data layer** (`src/features/oral-dose/api.ts` + `hooks.ts`): `oral_dose_logs` CRUD (table from migration 018) — `logOralDose`, `fetchRecentOralDoseLogs`, `deleteOralDoseLog`; `useOralDoseLogs` + `useLogOralDose` (invalidates `today-profile`, fires `ORAL_DOSE_LOGGED` analytics, schedules the 30-min absorption-clear notification when the oral reminder toggle is on). **DoseWindowCard** (`src/components/today/dose-window-card.tsx`): 3-state card with optimistic "Took it" (local timestamp starts the countdown before the refetch lands), a live per-second countdown that ticks **only inside the card** (1s while absorbing, 30s otherwise — whole Today screen stays still), state-aware accent (primary→amber→emerald), adherence streak pill, tier-2 `DisclaimerBanner` (Rule 8). Mounted at the **top of Daily Actions for oral users only**. **Today wiring:** `lastDoseDate` now flows from `oral_dose_logs` into the oral phase (`dose_due / dose_missed / building / steady_state`) replacing the Phase 1 `null` placeholder; `today/api.ts` fetches `dose_time_local`; `MedLevelBanner` route-gated **off** for oral users (it reads `injection_logs`). **Notifications** (`notifications.ts`): `scheduleOralDoseReminder(doseTimeLocal)` (DAILY trigger at the user's chosen time) + `scheduleAbsorptionClear()` (30-min TIME_INTERVAL one-shot); `NotificationId` extended; `parseLocalTime` helper for the Postgres TIME string. `use-notification-settings.ts` made route-aware (oral users get a "Daily dose reminder" toggle instead of "Injection day reminder"); Settings screen renders the route-correct row. **i18n:** new `oral_dose.*` namespace + `settings.notif_oral_dose*` (en/es parity). All copy em-dash-free, educational only. 14 files, 982 insertions. jest 54, vitest 464 (+20), type-check 0, lint 0 errors, translations parity ok. ATTORNEY REVIEW REQUIRED for `oral_dose.*` copy + the two oral notification bodies before any non-dev deploy. **Native note:** `scheduleAbsorptionClear` uses `TIME_INTERVAL` which works on the existing dev build; on-device test pending a build with an oral account. | ✅ |
| 76 | check-all GREEN + GitHub Actions CI + NativeWind purge (`3fc2357`→`1b0c5f3`, session 40). Code-health push that turned into a real cleanup. **(1) check-all green:** it was red only because eslint had **32 errors** (warnings don't fail it) — auto-fixed 23 (`3fc2357`), hand-fixed 9 (`e8cc328`: react-hooks purity via stable `useState`/timeout-driven `now`, a real missing `isPro` dep in visit-prep, one justified `set-state-in-effect` disable, type-redeclare alias, i18next `{{n}}`→`{{day}}` since the custom validator regex needs 2+ char vars). **(2) CI (`550f7a2`):** `.github/workflows/ci.yml` runs `check-all` + vitest on PR + push to master (ubuntu, Node 22, pnpm 10 via packageManager — NOT a `version:` input, which conflicts; `6c3d013`). **(3) NativeWind purge:** CI's clean Linux install surfaced `className` on RN components that local Windows `tsc` masked (stale node_modules carried the augmentation). Deleted the forbidden Obytes **Style tab** (`173bec4`) then the **feed demo + routes, `onboarding-screen`, and `checkbox`/Radio/Switch** primitive (`1b0c5f3`); converted the inert `className` (NativeWind not installed → ignored at runtime) to StyleSheet in the live Select chain (`modal`/`list`/`caret-down`) + the 404 screen. **Lesson:** CI on a clean checkout is the source of truth — local green ≠ CI green when node_modules drift. **Process:** shipped via a `ci-setup` branch + PR (the gate's `pull_request` trigger); CI went red→red→green across the iteration (pnpm conflict → real type error → all clean). 847 net deletions. Final: check-all EXIT 0, type-check 0, lint 0 errors / 220 warnings, **jest 54** (−9 deleted checkbox.test), **vitest 422**. PENDING: merge the PR into master. | ✅ |
| 75 | Nutrition screen reorder — meal chips removed + Recent Foods moved into the logging zone (`fa2090c` + `9a3e85b`, session 40). Tile-order pass right after shipping Recent Foods (entry 74). **(1) Meal-filter chips removed entirely** — deleted `src/components/log/meal-chip-row.tsx` and stripped from `log.tsx`: the `MealChipRow` import + `MealSlot` type, the `getMealSlot()` helper, `selectedMeal` state, the `filteredLogs` computation (FlatList now reads `logs` directly), the meal-name branch of `sectionLabel`, the `<MealChipRow>` render, and the now-impossible "No entries for [meal] yet" filtered empty state. Rationale: the chips were a pure *display* filter over a single day's list — they wrote no data (meal slot was derived from `logged_at` time, never stored) and never affected logging, so they didn't earn the top-of-screen slot. No i18n keys to remove (chip labels were hardcoded English in the component). **(2) RecentFoodsRow moved up** from the results cluster (it had shipped at ~#10, below the Daily Macro + Micronutrient cards) into the logging zone, directly below the Photo scan button. **New order:** Header → Voice hero → Photo scan → Recent Foods → Manual/Barcode toggle → manual form → Daily Macro → Micronutrient Watch → Today's Log. The `useRecentFoods`/`useRelogFoodEntry` hooks + `handleRelog` were unchanged (JSX position only). File header layout comment updated to match. **Decisions/patterns:** (a) a control that only filters a short single-day display and feeds nothing back into logging gets removed, not relocated; (b) logging actions live in the logging zone, results in the results zone — a free logging action (Recents) sitting beneath two results cards violated the screen's own "logging-first" philosophy; (c) conditionally-rendered tiles (`RecentFoodsRow` renders null with no history) can sit high safely — fresh accounts still open on the Voice hero, returning users get their fastest path up top. Voice hero kept as the visual lead per user choice (Recents below Photo, not above Voice). No migration, no edge/native change. type-check 0, jest 63, Vitest 422. PENDING on-device check + `pnpm ota:dev`. | ✅ |
| 74 | Recent Foods one-tap re-log — Scan Accuracy & Cost Cascade item A (`c3bba40`). First build of the committed cascade roadmap (session 40); the highest-leverage remaining lever now that 9 of 13 strategy points were found already shipped. **The win:** a horizontal quick-add bar of the user's most-eaten foods on the Nutrition Log screen, re-logged in one tap with **no AI call** — free, instant, exactly accurate (reuses the user's own last-confirmed macros). Eliminates an estimated 40-60% of expensive GPT-4o photo calls for repeat eaters once they have a routine. **What ships:** new pure helper `src/features/food-log/recent-foods.ts` — `normalizeFoodName()` (extracts the inline `.toLowerCase().trim()` used by `upsertFoodDefault`/`getFoodDefault`), `RecentFood` type (full macro/micro shape + `count` + `lastLoggedAt`), and `deriveRecentFoods(logs, {limit=8})` which dedupes a 30-day window by normalized name, keeps the MOST RECENT entry's macros per food, and ranks **smart blend** = frequency desc with recency as the tiebreak (user-chosen over recent-only and frequency-only). 10 Vitest cases (dedupe, recency-wins-on-merge, frequency rank, tiebreak, cap, source provenance, blank-name skip, empty input). New `relogFoodEntry()` in `api.ts` inserts one `food_logs` row at `now` (correct — eating it again now, so NO insert-signature changes needed across the codebase), copies all macros/micros, **preserves the original `source`** (a re-logged barcode item still reads `source: 'barcode'`) + `barcode_ean`. New hooks in `hooks.ts`: `useRecentFoods()` (queries 30-day range via the pre-existing `fetchFoodLogsInRange`, pipes through `deriveRecentFoods`; `foodLogKeys.recent` key) + `useRelogFoodEntry()` (invalidates today + recent, fires `FOOD_LOGGED_RELOG` analytics); **added `recent` invalidation to all 3 existing insert mutations** so a freshly logged food bubbles into Recents immediately. New `src/components/log/recent-foods-row.tsx` — `RECENT FOODS` section header + horizontal `FlatList` of 150px quick-add cards (name, serving, primary-light protein pill, `+ Log` CTA), one-tap fires `onRelog` + `haptics.success()` + a brief `✓ Added` check flash on the tapped card; **renders nothing when there's no history** (fresh accounts see no clutter). Wired into `log.tsx` between `MicronutrientWatchCard` and the Today's Log header; **not Pro-gated** (free, no AI). New `FOOD_LOGGED_RELOG` analytics event. 3 i18n keys (`log.recents_title/relog/logged`) en/es parity (54=54), Spanish `Añadido` diacritic correct, all copy em-dash-free. **No migration, no RLS change** (reuses `food_logs` policies), no AI, no attorney gate. **Decision/pattern:** for any new "quick repeat" surface, prefer reconstructing from the user's own confirmed history over re-running AI — it's the cheapest + most accurate path in the cascade. type-check 0, Vitest 422 (+10), jest 63. PENDING on-device smoke test + `pnpm ota:dev`. **Next cascade item: B (low-confidence confirmation nudge).** | ✅ |
| 73 | AI Data & Privacy disclaimer before first scan (`dfc82ac`). User saw a competitor's "Before your first scan" disclosure screen and wanted the same idea on-theme for GLiPra. **Distinct from the first-launch consent flow** (ToS / Medical Disclaimer / Privacy Policy shipped in session 17): those are broad legal gates at app entry; this is a **contextual disclosure at the AI-action moment** explaining what specifically happens to the photo or audio. Industry pattern for AI-powered consumer apps. **What ships:** new `useAiPrivacyAck` hook (AsyncStorage flag `glipra_ai_scan_data_privacy_ack`, same pattern as the daily-guidance disclaimer-seen key from entry 46) and new `AiPrivacyDisclaimerModal` component — full-screen Modal with `LinearGradient` hero matching the AnalyzingModal palette (visual continuity into the analyzing flow that opens immediately after), 4-bullet "What happens when you scan" numbered list, emerald "Data not used for ads or training" trust callout, amber "Not medical advice" Rule-8 reminder, two privacy links at the bottom (in-app `/legal/privacy-policy` via `router.push` + external `openai.com/policies/api-data-usage-policies` via `Linking.openURL` — matches the competitor's two-link pattern but with our actual provider), "I understand. Continue" primary + "Cancel" secondary buttons. **One-time per user, single combined modal:** one flag covers both photo and voice — chose this over per-source gates because the user is going to use both eventually and getting it out of the way once is cleaner than two consent prompts. **Trigger points:** photo gates inside `handleAnalyze()` in `log.tsx` (after Analyze tap, after the comment sheet); voice gates via new `onBeforeRecord?: () => Promise<boolean>` prop on `VoiceCaptureButton` — fires AFTER Pro check + mic permission grant but BEFORE the mic actually activates, so a declined disclaimer doesn't leave the user with an orphan recording. **Cancel = no scan AND no flag set** — user is re-prompted next attempt. **Resume pattern:** `pendingPhotoRef` stashes the capture + comment; `pendingVoiceResolverRef` holds a promise resolver the voice button awaits; on ack the deferred flow resumes; on cancel both are cleared. **Data-flow truth verified** before writing copy: `recognize-food` and `transcribe-food` both pass base64 through to OpenAI without persisting to Supabase Storage; only `ai_invocations` audit rows (user_id, function_name, model, tokens_used) are kept. OpenAI API terms confirm images/audio aren't used for training and are retained 30 days for abuse monitoring then deleted. **New `ai_privacy` i18n namespace, 15 keys per locale (en/es parity).** All copy em-dash-free per CLAUDE.md. **Attorney-review queue addition:** the new `ai_privacy.*` copy joins the existing list (`ai-coach` prompts, `EscalationCard`, `daily-guidance`, `pro_insight.subline_*`, delete-account modal); must clear attorney review before preview/production deploy. type-check 0, jest 63. **Second consecutive clean first-try OTA via `pnpm ota:dev`** (validates the Hermes hardening from entry 70 is holding). OTA group `7bedaefa-7aba-4461-8e46-0763032ac12c`. | ✅ |
| 72 | Onboarding / measurement units default to imperial (`4446fd3`). User feedback: "kg and cm should not be shown first" for the US-launching GLiPra. Flipped two `useState` defaults in `src/lib/unit-preference.ts` — `useWeightUnit` default `'kg'` → `'lbs'`, `useHeightUnit` default `'metric'` → `'imperial'`. AsyncStorage persistence is unchanged so anyone who previously saved a kg/metric preference keeps it — only the first-launch default changes. Also reordered the visual toggle so imperial appears first on all three screens that use it: onboarding `body.tsx` (weight `['kg','lbs']` → `['lbs','kg']` and height `['cm','ft · in']` → `['ft · in','cm']`), `(app)/weight.tsx` (track weight), and `(app)/goal-weight.tsx`. **Storage convention unchanged:** all weights still stored in kg internally and heights in cm; `lbsToKg` / `ftInToCm` convert at the input boundary; no DB migration needed. **Decision:** rule-of-thumb default for any new US-targeted measurement input is imperial-first / metric-second on the toggle order AND as the useState default; users who prefer metric flip once and the preference persists. type-check 0, jest 63. Delivered by OTA group `679f20c9-b2e6-4afd-ac19-0c0ba50fcfe0` (same OTA that delivered entry 71 since the onboarding commit landed on disk before the bundle finalised; a second no-op OTA was incidentally kicked, no harm). | ✅ |
| 71 | Numeric confidence chip on AIReviewSheet (`e33a9c7`). Replaces the HIGH / MEDIUM / LOW bucket chip with a `~XX%` badge that reads cleaner and lets users build calibration over time ("AI says ~70%, I should double-check the protein"). The leading tilde signals AI self-report — GPT-4o's confidence is loosely correlated with correctness but not statistically calibrated, same convention competitor apps use. **Backward-compat schema strategy:** added `confidencePercent: z.number().min(0).max(100).optional()` to both `recognize-food` and `transcribe-food` Zod output schemas; kept the existing `confidence: enum` so older clients still work. Client always reads `result.confidencePercent ?? bucketToPercent(result.confidence)` — the `bucketToPercent` helper in `photo-recognition.ts` maps `high → 90`, `medium → 65`, `low → 35` for the legacy fallback. GPT-4o prompts updated with calibration guidance (`85+ if clearly visible common dish; 60–84 if uncertain about portion or ingredients; 40–59 if unusual or partially obscured; under 40 if guessing`). Chip color still uses bucket thresholds (`≥80 green`, `50–79 amber`, `<50 red`) so the visual hierarchy is unchanged — only the label text moves from "Medium confidence" to "~65%". `MOCK_MEAL_RECOGNITION` already had `confidence: 0.87`; added explicit `confidence_percent: 87`. `MOCK_VOICE_PARSE` was just `'medium'`; added `confidencePercent: 65`. Edge functions deployed cleanly to `cuxndkreewlcmijxlgyg`. **Decision/pattern:** for any AI confidence surface, prefer numeric with a tilde over bucketed labels — same display effort, more user calibration value. Honest about the "~" because self-reported confidence is not a probability. type-check 0, jest 63. OTA group `679f20c9-b2e6-4afd-ac19-0c0ba50fcfe0`. | ✅ |
| 70 | AnalyzingModal — code-review polish + Windows EAS hardening (`74c3164` + `6b70ef8` + `832f754`). Three follow-up commits after shipping the AnalyzingModal (entry 69): (a) code-review pass on the just-shipped files surfaced five real fixes; (b) the recurring Windows Hermes bundler failures investigated to root cause + mitigated with safe config tweaks; (c) Windows EAS Update runbook codified in ARCHITECTURE. **Commit 1 — code-review fixes (`74c3164`):** (B1) wrapped `handleAnalyzingCancel`/`handleAnalyzingRetry`/`handleAnalyzingComplete` in `React.useCallback` in `log.tsx` — without this the modal's 350ms onComplete timer's useEffect deps churned every parent render, fragile if not currently broken; (B2) `useMemo` for the `data:image/jpeg;base64,...` URI in `analyzing-modal.tsx` — was rebuilt every render (~1.3MB string concat for a 1MB photo, allocation-only no decode); (B3) added `activeIndex !== -1` guard to the reset-effect in `use-analyzing-stages.ts` — prevents a stale reset firing in the microtask gap between isLoading-flipping-false and hasResult-flipping-true; (B4) animated the `WaveformGlyph` voice variant — 5 bars now scale on a staggered Reanimated `withRepeat` loop (worklet-driven, zero JS-thread churn), was static; (B5) replaced per-row `accessibilityLiveRegion="polite"` with a single visually-hidden live region near the top of the modal — VoiceOver/TalkBack don't reliably re-trigger announcements when the live attribute moves between rows; one source-of-truth view updates with the current stage label. **Commit 2 — Hermes hardening (`6b70ef8`):** after a research agent root-caused the 4× recurring Windows Hermes failures (debug-build `hermesc.exe`, unsupported Node 24, OneDrive file-locking races, default CPU-count workers, unpinned `hermes-parser`), applied four safe config mitigations: (H1) new `.npmrc` with `node-linker=hoisted` + `strict-peer-dependencies=false` to consolidate resolution; (H2) `package.json` `pnpm.overrides` pins `hermes-parser` to `0.32.1` — verified post-reinstall: was 4 distinct versions (0.25.1 / 0.29.1 / 0.32.0 / 0.32.1) coexisting in node_modules, now 1; (H3) `metro.config.js` caps `maxWorkers = 2` on Windows only (`process.platform === 'win32'` guard, Linux EAS cloud unaffected) — eliminates the `0xC0000142` init race when many `hermesc.exe` children spawn together; (H4) new `pnpm ota:dev` and `pnpm ota:dev:clear` scripts wrap `eas update` with `EXPO_USE_FAST_RESOLVER=1` — reduces filesystem churn during sourcemap merge. **The canonical OTA invocation is now `pnpm ota:dev` (or `:clear` for cache wipe), replacing the bare `pnpm exec eas update ...` calls.** **Commit 3 — ARCHITECTURE runbook (`832f754`):** added a dedicated "Windows EAS Update runbook" section after the decisions log with: 5 confirmed contributing factors, the new failure-escalation ladder (plain retry → cache wipe → retry → code investigation → deferred mitigations), 2 flagged user-decision items (move repo off OneDrive, downgrade local Node to 22 LTS — both deferred per user, NOT applied), 5 related GitHub issue links for future debugging. **Deferred user-decision items** (acknowledged, not applied): repo-off-OneDrive and Node-22-LTS are the heaviest mitigations available but remain backlog. type-check 0, jest 63, Vitest 401. **OTA via the new `pnpm ota:dev` pipeline succeeded first-try** (validates H1–H4 took effect — first clean OTA in 4 attempts this session): group `dd26612d-499d-4f2a-8635-ec5d6b926659`. | ✅ |
| 69 | AnalyzingModal — staged checklist for photo + voice AI calls (`6e12f94`). User saw a competitor's post-snap loading screen (5-stage checklist + image thumbnail + slow-connection hint + Cancel) and wanted the same idea for GLiPra. Mocked at `.planning/mockups/analyzing-modal.html`. **Three problems solved in one ship:** (a) replaces the flat inline-spinner-on-the-button loading UX with a premium full-screen modal — "perceived progress" via 5 stages instead of an empty wait; (b) **closes todo item C** (silent photo failure) — when `recognize-food` errors, the active stage gets a red `!` icon and an error block + Try again button appear instead of the spinner just stopping; (c) **adds a real Cancel** — `AbortSignal` raced against the in-flight `supabase.functions.invoke()` lets the user bail out at any time. **What ships:** new pure helper `src/features/food-log/analyzing-stages.ts` with stage definitions per source (5-stage list each: photo = image_received / identifying_food / calculating_macros / checking_glp_nutrients / building_pro_insight; voice = audio_received / transcribing_voice / identifying_food / calculating_macros / building_pro_insight), `MIN_STAGE_DONE_MS=250` visibility floor, `planNextStageDelay()` for natural cadence (~0.8s / 1.2s / 1.5s ticks then hold on last), `planDrainDelay()` for accelerated catch-up once response lands, `SLOW_HINT_AFTER_MS=8000` threshold. 13 Vitest cases. New `useAnalyzingStages` hook orchestrates the active-index advancement: ticks at natural cadence while the call is in-flight, drains remaining stages after response lands (each gets ≥250ms "done" visibility before next ticks — prevents the "slam stages all-done at once" feel on fast responses), fires `showSlowHint` after the 8s threshold when stuck on the last stage. New `AnalyzingModal` component: full-screen `Modal` with a `LinearGradient` hero (`gradients.hero` purple→blue→teal for photo source, always-dark `#1e1b4b → #312e81 → #1e3a8a` for voice matching the existing voice-hero card), 110×110 rounded thumbnail (real photo or animated 5-bar waveform glyph for voice), 5-stage checklist with brand-purple active row + emerald done checkmarks + red error `!` icon, optional amber slow-hint block, optional red error block, Cancel button (or Close + Try again in error state). `accessibilityLiveRegion="polite"` on the active row + descriptive `accessibilityLabel` so VoiceOver/TalkBack announces transitions. **AbortSignal threading:** new `raceAgainstAbort()` helper in `photo-recognition.ts` races `supabase.functions.invoke()` against a signal — supabase-js v2 doesn't natively propagate signals to the underlying fetch, so the orphan call continues server-side after cancel but the UI unblocks immediately. One `ai_invocations` quota slot is still spent for an aborted call; acceptable trade vs. forking a custom fetch path. Same pattern inlined in `voice-recognition.ts`. **Voice return type changed** from `RecognitionResult` to `RecognitionResult | null` (null on abort); test updates only. **log.tsx orchestration:** single `analyzingSource: 'photo' \| 'voice' \| null` state, one `AbortController` ref recreated per call (cancelling any in-flight previous), shared modal renders for either source, AIReviewSheet now gates on a `modalComplete` flag so it only opens after the green-check drain finishes. PhotoCaptureButton + VoiceCaptureButton inline-spinner props read from `analyzingSource` instead of the old per-source loading flags. **Six refinements locked in during plan review:** (1) no false success button — modal auto-closes; (2) adaptive pacing instead of fixed timers; (3) softer slow-hint copy ("Hang tight. This can take up to a minute on slow connections.") leading with reassurance; (4) error block doesn't promise the search-database feature until Move 5 ships; (5) screen-reader live region; (6) bigger 110×110 image thumb. **New `analyzing` i18n namespace** in en/es (parity, ~25 keys each: stage labels, hero titles, error copy, slow hint, accessibility states). **All copy em-dash-free** per CLAUDE.md rule. type-check 0, jest 63, Vitest 401 (+13 new). **Recurring bundler footgun:** OTA hit Windows Hermes segfault (exit 3221225794 = 0xC0000142) on first attempt despite cache wipe; retried clean. Pattern now seen 4x this session — codifying in ARCHITECTURE as "first-attempt Hermes segfaults on Windows are sticky; always retry once before code surgery". OTA group `efd57abb-060a-444f-9e14-0651d29ca0a9`. | ✅ |
| 68 | AI Review Sheet — portion multiplier pills (`aad36a2`). Move 2 from the AI-review-sheet backlog (Move 1 was Pro Insight in entry 67). User wanted a way to say "this was actually 1.5 servings" without retyping 4 macro fields. Mocked two directions in `.planning/mockups/portion-multiplier.html` — segmented pills (Direction A) vs snap slider (Direction B) — and shipped pills because they're 2–3× faster per change (single tap vs find-then-drag), tap targets are ~84px wide instead of a 24px slider thumb, no stray-drag risk, and the discrete-only snap behaviour is honest about what the control does (a slider implies continuity that doesn't exist between ½× / 1× / 1½× / 2×). **Files:** new pure helper `portion-multiplier-helpers.ts` exporting `scaleMacros(base, multiplier)` → form-shaped string values matching the existing decimal-vs-integer precision (`proteinG/carbsG/fatG/fiberG/b12Mcg/zincMg` → `.toFixed(1)`, `caloriesKcal/vitaminDIu/magnesiumMg` → `.toString()`), `deriveFieldBase(currentString, multiplier)` to recover a field's base after a manual edit, and `snapToMultiplier(raw)` for future slider use. New `PortionMultiplier` component renders 4 segmented pills (`flex: 1` row, gap `spacing.sm`) with `accessibilityRole='radiogroup'` + per-pill `accessibilityRole='radio'` + `accessibilityState.selected`; active pill = `colors.primary` fill + `shadows.sm`, inactive = surface + 1px `colors.border` outline; `haptics.tap()` on each press; readout row shows the current multiplier (`1×`/`½×`/etc.) + scaled `{{kcal}} kcal · {{proteinG}}g protein` preview. `AIReviewSheet` wires two new pieces of state alongside the existing `form`: `aiBase: MacroBase | null` (the immutable AI snapshot) and `multiplier: PortionMultiplierValue`. On multiplier change, all 9 numeric form fields rewrite from `aiBase × multiplier` via `scaleMacros`. On manual edit to a numeric field, that field's `aiBase` re-derives via `deriveFieldBase(value, currentMultiplier)` so subsequent multiplier moves scale around the user's correction, not the AI's original. Non-numeric fields (name, serving description) pass through unchanged. New `extractMacroBase()` helper snapshots the numeric fields off either `RecognitionResult` or `PhotoFoodEntry` (defaults path), since both have the same field shapes. **Save path unchanged** — existing `parseEntry(form)` sees the already-scaled strings; no save-time math. **Pro Insight headline auto-updates** as the multiplier changes (it reads `form.proteinG` which scales live); no wiring change needed on that side. **Null fields stay empty** — `scaleMacros` leaves `null` base values as `""` in the form, so "AI didn't know fiber" doesn't become "1.5× of unknown". **i18n:** new `portion_multiplier` namespace (en/es parity, 4 keys: `label`, `readout_with_kcal`, `readout_protein_only`, `pill_a11y`). 17 new Vitest cases covering identity at 1×, scaling at 0.5/1.5/2×, null-field passthrough, defensive cases (zero multiplier, negative base values), and round-trip via `deriveFieldBase`. type-check 0, jest 63, Vitest +17 (388 total). Shipped via OTA `--clear-cache` (Hermes-minifier playbook from entry 67). OTA group `2b24bcc1-62eb-4e5b-9164-b62d7af5204a`. | ✅ |
| 67 | AI Review Sheet — Pro Insight card (`612c81a`). User saw a competitor's "PRO INSIGHT" callout on their post-photo review screen and wanted the same idea, Pro-gated, but on-theme for GLiPra. Mocked 6 borrowed moves in `.planning/mockups/ai-review-sheet.html`; user picked Move 1 only (Pro Insight) to ship first so things stay clean. **Naming:** original "PHARMACIST INSIGHT" rejected by user as credentialing overclaim (CLAUDE.md Liability Rule 2 controls pharmacist language); locked on `PRO INSIGHT` instead, which is honest about the gating and avoids implying clinical authority over a static `floor - consumed` calculation. **What ships:** new pure helper `src/features/food-log/pro-insight-helpers.ts` exporting `composeInsight()` that takes `{ proteinConsumedG, mealProteinG, proteinFloorG, phase, daysSinceInjection }` and returns `{ headlineKey, headlineVars, sublineKey, sublineVars } | null`. Three headline branches: `under_floor` (`Brings you to 87g. 33g to go to hit your floor.`), `at_floor` (within ±2g tolerance, `You hit your floor. 120g protein today.`), `over_floor` (`Past your floor by 10g. Strong day.`). Five sublines, one per `InjectionPhase`: `injection_day` / `peak_suppression` / `adjustment` / `recovery_window` / `overdue` — each describes typical appetite patterns + protein food suggestions only, **no medication advice**. Suppression rules: returns `null` for no floor, zero floor, or both consumed+meal=0. Component-level extra suppression for `medicationStatus === 'discontinued'`. New `ProInsightCard` (`pro-insight-card.tsx`): Pro variant is a full insight card (brand-purple top accent + primary-light background + 6px brand dot + `PRO INSIGHT` label + headline + subline), free variant is a one-line teaser ("Unlock Pro Insights. See how each meal fits your floor.") that opens the paywall via `RevenueCatUI.presentPaywallIfNeeded({ requiredEntitlementIdentifier: 'GLiPra Pro' })`. Dropped between the GLP-1 Watch grid and the Cancel/Log It buttons in `ai-review-sheet.tsx`; consumes `form.proteinG` so the headline updates live as the user edits. **No Rx monogram on this feature** (the Rx mark stays concentrated on the pharmacist-authored content cards where it's earned) and **no Pro pill** (redundant with a label that already says PRO). **i18n:** new `pro_insight` namespace, 10 keys per language (en/es parity). All copy verified em-dash-free per CLAUDE.md rule. **Tests:** 19 new Vitest cases in `pro-insight-helpers.test.ts` covering all suppression branches, all 3 headline classifications, all 5 phase sublines, rounding, null/negative day handling, and exhaustiveness via never-narrowed default. **Out of scope (deferred):** Move 2 (portion slider), Move 3 (numeric confidence), Move 4 (Rescan), Move 5 (manual food search), Move 6 (other estimates). Each gets its own session. **Attorney-review gate flagged:** the `pro_insight.subline_*` copy describes appetite patterns and goes through the same legal review as `ai-coach` prompts and `EscalationCard` before any preview/production deploy. **Bundler note:** OTA bombed twice on Windows Hermes minification (RevenueCat package + transient segfault); resolved with `--clear-cache`. Pattern documented in ARCHITECTURE: when an EAS Update fails on Hermes bytecode generation, always try `--clear-cache` before suspecting code. type-check 0, jest 63, Vitest +19 (371 total). OTA group `ad7e3748-f57c-48a0-9147-bffbe68724f4`. | ✅ |
| 66 | Symptom Trends — polyline → severity heat strip (`403feb3`). The Progress screen's `CheckInSymptomCard` plotted nausea + energy as two SVG polylines on the same 1–5 axis. The polyline implementation filtered out null days and connected the rest, so a 2 → 4 jump across a missed day rendered as a vertical wall; users with sparse check-in history saw a jagged criss-cross. Brainstormed 5 directions in `.planning/mockups/symptom-trends.html` (sparkline split, heat strip, phase-correlation bars, insight-only cards, remove-entirely). User picked **heat strip** (Direction B) with C noted as a v2 once users have ≥2 full cycles of data. Built a new reusable `SeverityHeatStrip` primitive in `src/components/progress/severity-heat-strip.tsx`: takes `(number | null)[]` values + `max` + `palette` ('warningScale' | 'successScale'), chunks into fixed-width rows of `flex: 1` cells with `aspectRatio: 1` so the grid flexes with the parent card width, colours each cell from the appropriate 5-step token ramp (null → `colors.border` warm neutral so empty days are distinguishable from any severity level). Rewrote `check-in-symptom-card.tsx` as two heat-strip rows (Nausea / Energy) each with a header (`avg 2.1 · lower is better` / `avg 3.9 · higher is better`) and a shared inline legend at the bottom (`mild ▢▢▢▢▢▢ severe   ▢ no check-in`). Pharmacist tip footer kept. **Token additions:** `scales.warningScale` (amber 100→700 ramp) and `scales.successScale` (emerald 200→700 ramp) in `GlipraTokens`, both palettes (darker variants for dark mode). **Window-aware sizing:** new `cellsPerRowFor(width, days)` picks 7 cells/row at 7 days, 15 at 30, 18 at 90, etc, so each cell stays roughly tappable across range selections. **i18n:** added `progress.symptoms_card.{avg, lower_is_better, higher_is_better, legend_mild, legend_severe, legend_no_data}` in en/es. **Code comment** in `CheckInSymptomCard` flags Direction C (phase-correlation bars) as the documented v2 path, with link to the plan file so it's not forgotten. type-check 0, jest 63. OTA group `07866360-6ea2-4bf6-83f7-ea66c30ce7ec`. | ✅ |
| 60 | Brand rename "Glipra" → "GLiPra" (user-facing only, `00cc02a`). Capitalized the brand as GLiPra everywhere users see it (to evoke GLP-1), across 25 files: app display NAME (`env.ts`), en/es translation values, welcome/consent/onboarding-reveal/health-import copy, paywall + pro-gate "GLiPra Pro", content-card bodies, milestone share text, both legal screens + `LegalDocScreen` footer, camera + notification-permission alerts, the injection-reminder notification body, and the published website (`docs/*.html` + `docs/legal/*.md`). Method: guarded `sed 's/Glipra\([^A-Za-z]\)/GLiPra\1/g'` (leaves `Glipra`-identifiers, which are always followed by a letter) on rendered-string files + targeted edits for `env.ts`/notifications to avoid comments; a grep guard caught two missed files (`paywall.tsx`, `reveal.tsx`). **Deliberately NOT changed** (would break things / invisible to users): code identifiers (`GlipraTokens`/`GlipraTabBar`/`GlipraThemeProvider`, 89 files still use them), `com.glipra.*` bundle IDs, schemes, EAS slug, package name, `glipra.com` domain, all `@glipra.com` emails, the Open Food Facts `User-Agent: Glipra/1.0` header, and code comments. type-check 0, lint:translations parity, 63 jest + 363 Vitest. Shipped: OTA to `development` (group `32cf5f79`) for in-app text + Pages push for the website. NOTE: the home-screen app label flips to "GLiPra" only on the next native EAS build (it's baked into the manifest). | ✅ |

### Correction note (2026-05-30 audit)

A code audit against committed HEAD `81b2433` found that sessions **42, 43, 44, 45** were
logged ahead of the code: their descriptions were written, but the consuming component
changes were never committed to this repo. Only the redesign's translation keys landed,
which are now **dead code**: `voice_hero_title`, `voice_hero_subtitle`, `voice_cta`, and
`free_logging_note` exist in `en.json`/`es.json` but no component references them.

The build currently under on-device test (`81b2433`) ships the **session-41 voice UI**
(side-by-side Photo/Voice buttons, compact mic button) and the **session-46 Daily AI
Guidance** feature only. Sessions 41 and 46 are verified accurate. The voice-hero redesign
and token cleanup remain to be built (see Direction B backlog below if re-prioritized).

**Resolved (session 39):** the voice-hero redesign and the "always free" note were finally
implemented and committed (entry 53, `bd7eb76`) — the previously-dead keys (`voice_hero_title`,
`voice_hero_subtitle`, `voice_cta`, `free_logging_note`) are now live and rendered. The
VoiceCaptureButton hardcoded-hex → semantic-token cleanup (entry 42) was also completed
(entry 57, `9600292`): all colors are now `tokens.ts` values, zero literals remain. The entire
session-42/43/44/45 backlog from that era is now closed.

### Upcoming — Visual Redesign (Direction B)

Design direction locked in session 14 brainstorm. Implement in order:

| # | Item | Priority |
|---|---|---|
| D1 | ThemeContext — light/dark token system replacing hardcoded colors.ts | ✅ |
| D2 | Direction B gradient hero — Today screen header with purple→blue gradient | ✅ |
| D3 | ℞ Prescription pad SpotlightCard — purple header, ruled lines, Sig: label, stamped footer | ✅ |
| D4 | Pill strip injection cycle card — 7-cell blister pack, phase colors, today ring | ✅ |
| D5 | Custom tab bar — gradient active pill + dot indicator, light + dark | ✅ |
| D6 | Micro-animations — protein ring fill (spring), readiness score count-up, streak pop-in | ✅ |
| D7 | Skeleton loading states — shimmer ghost cards replacing ActivityIndicator | ✅ |
| D8 | Onboarding gradient hero — Direction B carried through all onboarding screens | ✅ |
| D9 | Milestone card gradient upgrade — unlocked achievements get gradient card treatment | ✅ |
| D10 | Protein floor reveal card — big 52px number with formula breakdown, ℞ watermark | ✅ |

---

## Month 2 — Core Tracking (complete)

1. Food logging (manual entry + barcode scanner via expo-camera)
2. Daily check-ins (nausea 1-5, energy 1-5, water tracker)
3. Weight tracking + EWMA trend chart (SVG, react-native-svg)
4. Streaks gamification (STREAK_THRESHOLD = 0.80)
5. 10 pharmacist-authored content cards carousel on Today screen
6. Readiness score wired to live check-in data

---

## Month 1 — Foundation (complete)

1. Auth (email + Apple Sign In)
2. First-launch consent flow (ToS + Medical Disclaimer + Privacy Policy)
3. 10-step onboarding (medication → injection day → body → safety → dietary → goals → status → protein floor → import → reveal)
4. Protein floor calculator with safety bounds + acknowledgment modal
5. Injection cycle calculator
6. Today screen skeleton (Readiness Score + protein ring + phase badge)

---

## Stack Additions (post-scaffold)

| Package | Purpose |
|---|---|
| expo-camera 17.0.10 | Barcode scanning |
| expo-image-picker 17.0.11 | Photo food recognition |
| expo-haptics 15.0.8 | Welcome screen tap feedback |
| @react-native-community/datetimepicker | Date/time pickers in Add Shot (requires new EAS build) |
| expo-av ~16.0.8 | Audio recording for voice food logging |
