# On-Device Test Checklist — build 96405614

**Build:** EAS development, Android, commit `efb5763`
**Install:** https://expo.dev/accounts/waliabdul/projects/glipra/builds/96405614-b80d-4b1d-9e3c-081322a0068a
**Date:** 2026-05-31

## Behavior of this build (read first)
- `EXPO_PUBLIC_USE_MOCK_AI=true` → **Pro is auto-unlocked** (mock-dev override in use-subscription), so all Pro features are testable with no purchase/paywall.
- **AI is mocked** — voice/photo/guidance/coach return canned data. You're verifying native modules + UI flow, not real OpenAI output.
- **GDPR export/delete are REAL** — they call live Supabase edge functions. ⚠️ **Use a throwaway account to test Delete (irreversible).**

---

## A. Install & launch
- [ ] Install the APK over the existing dev client; app opens with no redbox.
- [ ] Open every tab (Today, Log/Nutrition, Progress, Sites, Coach, Settings) — no crash.
- [ ] No `requireNativeModule('ExponentAV')` crash on the Log tab (expo-av).

## B. GDPR — Export my data (Settings → Account)
- [ ] Tap "Export my data" → a share sheet opens with a `.json` file.
- [ ] Open/inspect the JSON: contains your rows across tables (profiles, food_logs, weight_logs, daily_checkins, injection_logs, etc.).
- [ ] Tap again immediately → second export within 24h is rate-limited (1/day).

## C. GDPR — Delete account (Settings → Account)  ⚠️ throwaway account
- [ ] Tap "Delete account" → type-to-confirm modal appears.
- [ ] Delete button stays disabled until you type `DELETE` (or `ELIMINAR` in Spanish).
- [ ] Confirm → app signs you out and returns to the auth/welcome screen.
- [ ] Try to log back in with that account → fails (user + all data gone).

## D. Voice logging (Pro, Log tab)
- [ ] Tap the Voice button → OS mic-permission prompt (first time).
- [ ] Deny → alert, no crash. Grant → recording state (waveform + MM:SS timer).
- [ ] Tap to stop → processing → AIReviewSheet shows the mock result with the transcript quoted.
- [ ] Save → entry appears in today's log; protein ring updates.
- [ ] Start recording then navigate away → no crash (unmount cleanup).

## E. Daily AI Guidance (Pro, Today tab)
- [ ] Gradient guidance card renders (expo-linear-gradient loads, no crash).
- [ ] First view shows the Tier-1 disclaimer; acknowledge it.
- [ ] Mock guidance text appears; "Why this?" expands/collapses the reasoning.
- [ ] Set medication status = discontinued (Settings) → card is suppressed.

## F. Barcode corrections (now persists — table is live)
- [ ] Scan a barcode (or use the manual/barcode path) → product loads.
- [ ] Edit a nutrition value (e.g. protein) and confirm/save.
- [ ] Re-scan the SAME barcode → it loads YOUR corrected values (proves the new `barcode_corrections` table works end-to-end).

## G. Regression / native modules
- [ ] Photo capture (expo-image-picker) → AIReviewSheet (mock).
- [ ] Manual food log + barcode scanner (expo-camera).
- [ ] Add Shot date/time picker works.
- [ ] AI Coach: send a message in mock mode → returns the mock reply, no crash (verifies the MOCK_COACH_REPLY fix).
- [ ] Notifications: toggle the reminders in Settings; if a notification fires, it displays (SDK-54 NotificationBehavior fix).
- [ ] Language EN ↔ ES switches copy in place; dark mode toggles cleanly.

---

## Notes
- For testing **real** AI output, a separate build with `EXPO_PUBLIC_USE_MOCK_AI=false` is needed — gated on attorney review of the AI prompts.
- RevenueCat purchase buttons won't complete (no store products configured yet) — expected; Pro is already unlocked via the mock-dev override, so you don't need them.
